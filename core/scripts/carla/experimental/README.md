# Experimental process files

This directory contains historical experiment orchestration, recovery,
monitoring and milestone-specific runners. Names containing internal stage or
version identifiers are retained only for reproducibility of past runs.

These files are not the public entry point. Use
`../run_closed_loop_experiment.sh` for the maintained closed-loop workflow.
