# Give-Way SMPC Experiment Changelog

This file records each give-way SMPC tuning change, the amount changed, observed effect, and the next decision. Before changing the experiment again, read this file first and avoid repeating a configuration that has already been tested.

## Current Rule

- Do not tune by memory only. Every tuning change must add or update one row in this changelog.
- Record both the parameter delta and the observed result directory.
- Compare against the nearest previous run, not only against the gate threshold.
- Keep `yield_activation_distance` and `yield_observed_caution_distance` fixed unless the changelog shows reference-profile tuning has stopped helping.
- Treat solver failures by phase:
  - `released_recovery` failures: tune recovery speed/accel or rejoin reference.
  - `approach_yield_line` failures: tune stop buffer or braking reference profile.
  - `hold_yield_line` failures: tune `yield_reference_min_speed` before moving the stop line or trigger distance.

## Key Metrics

- Required policies: `smpc_var_risk`, `smpc_fixed_risk`.
- Gate threshold: `solver_failure_frac <= 0.050`.
- Safety must remain true: completion valid, no footprint collision, target clears before ego enters conflict zone.

## Latest Local Change: Restore 20260628 Mainline Code/Config

Date: 2026-07-05

Trigger:

- User compared `20260628_103325_final_dissertation` videos and confirmed that this result has no visible post-turn lane change for `notv_cl` and `smpc_var_risk`.
- User requested cleaning unused visual-heading code and restarting from that successful mainline.

Evidence from `20260628_103325_final_dissertation`:

- The applied tuning config contains only the core safety/yield/recovery settings.
- Safety/yield config includes `yield_stop_buffer_distance=8.0` and `yield_release_clearance_margin=1.0`.
- `smpc_var_risk` completion happened at `step=232`, `goal_dist=7.913m`, `s=41.0m`, `end_s=48.0m`, `s_to_end=7.0m`, `ey=-3.135m`, `epsi=-0.010rad`, `completed_by_goal_dist=true`.
- Interpretation: the video ends while the ego is already straight after the turn and before later downstream route-tail correction can pull it into a visible lane change.

Cleanup performed:

- Reverted local `SMPCAgent` / scenario-runner changes for `exit_lane_lock` and `preview_terminal` wiring.
- Removed lane-entry visual-branch scripts:
  - `core/scripts/carla/run_lane_entry_heading_diagnostics_check.sh`
  - `core/scripts/carla/run_lane_entry_heading_cost_check.sh`
  - `core/scripts/carla/run_lane_entry_preview_steering_check.sh`
  - `core/scripts/summarize_lane_entry_heading_diagnostics.py`
- Restored `core/scripts/carla/scenarios/tuning_configs/give_way_smpc_tuning.json` to exactly match `core/results/20260628_103325_final_dissertation/applied_tuning_configs.json`.
- Replaced the post-turn heading plan with a deprecated-branch status note pointing back to the 20260628 mainline.

Decision:

- Use `run_give_way_final_dissertation_batch.sh` as the restart entry point.
- Do not use the lane-entry heading branch scripts or parameters.
- Treat `20260628_103325_final_dissertation` as the active baseline for the next full experiment expansion.
- Scope is intersection-only: do not expand this dissertation reproduction into the original paper's lane-change or hardware / vehicle-in-the-loop experiments unless the research scope explicitly changes.
- Keep the successful mainline risk profile as `adaptive_interaction_severity`; do not introduce a separate fixed-`epsilon=0.02` branch for the baseline/full experiment path.

Follow-up after server run `20260705_152245_final_dissertation`:

- Server result failed only because required SMPC policies did not write valid completion; both policies still had `solver_failure_frac=0.000`, no footprint collision, and yield order OK.
- Root cause: server code still used visual-heading branch defaults even though the tuning config had been restored:
  - `completion_goal_dist=4.0`
  - `completion_lateral_error=1.5`
  - `completion_heading_error=0.10`
  - `completion_s_margin=2.0`
  - `post_goal_reference_extension_m=12.0`
  - `exit_alignment_path_enabled=True`
- These defaults differ from the validated 20260628 completion behaviour and forced the ego to chase the post-goal route tail until 600 steps.
- Local fix:
  - `completion_goal_dist=8.0`
  - `completion_lateral_error=4.0`
  - `completion_heading_error=0.18`
  - `completion_s_margin=6.0`
  - `post_goal_reference_extension_m=0.0`
  - `exit_alignment_path_enabled=False`
- Recomputing the latest failed trajectory with the restored defaults shows expected valid completion:
  - `smpc_var_risk`: would complete at step 277, `goal_dist=7.926m`, `ey=-3.134m`, `epsi=0.002rad`.
  - `smpc_fixed_risk`: would complete at step 256, `goal_dist=7.881m`, `ey=-3.104m`, `epsi=0.005rad`.
- Decision: sync the corrected code defaults before rerunning the single-init baseline. Do not proceed to 50-init full experiments until this single-init rerun passes.

Follow-up for 50-init feasibility:

- Changed `core/scripts/carla/scenarios/inits/ego_init_01.json` from the tuned speed `6.0m/s` back to the original paper initial speed `9.364703726496288m/s`.
- The current single-init file now exactly matches `core/scripts/carla/scenarios/inits/paper_intersection_50/ego_init_01.json`.
- Purpose: validate the baseline under one original paper initial condition before running the full 50-init batch.
- Risk: this is a harder case than `20260628_103325_final_dissertation`, because the ego starts with substantially higher speed and may need earlier/more aggressive give-way braking.

## Change History

| Run / state | Change | Parameters | Observed effect | Decision |
|---|---|---|---|---|
| `20260620_223238` | Smooth braking-distance reference profile introduced before the later min-speed/recovery tuning. | `yield_stop_buffer_distance=8.0` era, smooth reference profile; exact current-style `yield_reference_decel` not yet separated. | Safe yield and completion passed. Solver failure was just above threshold: `var=0.055`, `fixed=0.055`. Footprint separation: `var=2.776m`, `fixed=2.839m`. | Good safety baseline, but EV behavior was visually too conservative/slow after yield. |
| `20260620_231320` | Tried making EV less conservative and faster after TV clears. | `yield_stop_buffer_distance=5.0`, `yield_recovery_speed=5.0`, `yield_recovery_accel=1.5`, activation/caution stayed near `12.0`. | Safety/completion still passed, but solver failure regressed badly: `var=0.165`, `fixed=0.083`. Footprint separation fell to `var=1.389m`, `fixed=2.149m`. | Do not repeat this aggressive combination. Stop line too close and recovery too aggressive. |
| `20260620_234829` | Backed off to a more stable stop line and slower recovery. | `yield_stop_buffer_distance=6.5`, `yield_recovery_speed=4.0`, `yield_recovery_accel=1.2`, release waits for target cleared. | Improved solver failure: `var=0.064`, `fixed=0.069`. Safety/completion passed. Footprint separation improved to about `3.0m`. | Effective direction. Remaining failures were in approach/hold, not recovery. |
| `20260621_153823` | Tried lowering parking distance slightly for visual naturalness. | `yield_stop_buffer_distance=6.0`, activation/caution unchanged at `12.0`, recovery unchanged. | Regressed: `var=0.099`, `fixed=0.069`. Footprint separation worsened to `var=1.981m`, `fixed=2.398m`. | Do not continue lowering stop buffer. `6.0m` is too close for var-risk feasibility. |
| `20260621_161906` | Split the difference and softened reference decel. | `yield_stop_buffer_distance=6.25`, `yield_reference_decel=-4.0`, `yield_reference_min_speed=0.8`. | Improved over `6.0m`: `var=0.090`, `fixed=0.070`. Safety passed, but still above threshold. | Direction helped but not enough; remaining failures in approach/hold. |
| `20260621_164134` | Further softened reference decel. | `yield_reference_decel=-3.5`, `yield_reference_min_speed=0.8`, stop buffer stayed `6.25`. | Clear improvement: `var=0.066`, `fixed=0.066`. Footprint separation `var=2.557m`, `fixed=2.545m`. | Best balanced tested run so far. Keep `-3.5` as baseline unless a new change beats it. |
| `20260621_181839` | Tried even softer reference decel. | `yield_reference_decel=-3.0`, `yield_reference_min_speed=0.8`, stop buffer stayed `6.25`. | Not worthwhile: `var=0.065` was only marginally better, but `fixed=0.075` regressed. Safety/completion still passed. | Do not continue softening decel. Revert to `-3.5`. |
| `20260621_192020` | Kept best decel and raised hold reference floor. | `yield_reference_decel=-3.5`, `yield_reference_min_speed=1.0`, stop buffer `6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | No improvement: `var=0.066` stayed flat, `fixed=0.085` regressed. Safety/completion still passed. Fixed-risk hold failures increased to 10; recovery remained stable with 0 failures. | Do not keep `yield_reference_min_speed=1.0`. Revert to `0.8` or test a smaller `0.9` only if needed. Current best measured run remains `20260621_164134`. |
| Local pending after `20260621_192020` | Reverted the rejected hold reference floor trial to the best measured baseline. | `yield_reference_min_speed=1.0 -> 0.8`; kept `yield_reference_decel=-3.5`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Local validation pending in this row; expected to recover fixed-risk behaviour toward `20260621_164134`. | Re-run CARLA to confirm the regression from `1.0` is removed before attempting new tuning. |
| `20260621_194245` | CARLA confirmation after reverting the rejected hold reference floor. | `yield_reference_min_speed=0.8`, `yield_reference_decel=-3.5`, `yield_stop_buffer_distance=6.25`, `yield_brake_distance_margin=3.0`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate still failed only on solver failure: `var=0.076`, `fixed=0.070`. Safety/completion/yield order passed; no footprint collision. Failures stayed in `approach_yield_line` and `hold_yield_line` (`var`: 9/7, `fixed`: 9/6); recovery had 0 failures. Fixed-risk recovered from the rejected `1.0` min-speed run, but the run did not beat the `20260621_164134` baseline. | Keep `yield_reference_min_speed=0.8`, but do not claim this is an improvement. Current best measured run remains `20260621_164134`. Next tuning should target early approach feasibility/debug, not recovery. |
| `20260621_200449` | Re-ran the restored baseline configuration. | `yield_reference_min_speed=0.8`, `yield_reference_decel=-3.5`, `yield_stop_buffer_distance=6.25`, `yield_brake_distance_margin=3.0`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate still failed only on solver failure: `var=0.066`, `fixed=0.070`. Safety/completion/yield order passed; no footprint collision. Failure phases improved for `var` versus `20260621_194245` (`var`: 8 approach / 6 hold), while `fixed` stayed at 9 approach / 6 hold. First failures are `INF_OR_UNBD` during early `approach_yield_line`, with fallback braking active. | Confirms the restored baseline is stable-ish but not enough. Do not change recovery. Next change should target early approach feasibility, preferably first-failure debug or a small `yield_brake_distance_margin` increase. |
| Local pending after `20260621_200449` | Increased brake-distance margin to enter yield braking slightly earlier. | `yield_brake_distance_margin=3.0 -> 3.5`; kept `yield_reference_decel=-3.5`, `yield_reference_min_speed=0.8`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Local validation pending. Expected effect: reduce early `approach_yield_line` `INF_OR_UNBD` solver failures by giving the optimiser more distance before the stop point, without moving the stop line or making activation/caution more conservative. | Run CARLA and compare against `20260621_200449`; success means both required policies move below or closer to `0.050` without hurting safety/completion. |
| `20260621_211650` | Server re-run did not include the pending brake-margin change. | Actual server config still had `yield_brake_distance_margin=3.0`, with `yield_reference_decel=-3.5`, `yield_reference_min_speed=0.8`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate failed only on solver failure: `var=0.070`, `fixed=0.079`. Safety/completion/yield order passed; no footprint collision. Failure phases stayed in early `approach_yield_line` and `hold_yield_line` (`var`: 9/6, `fixed`: 9/8), all `INF_OR_UNBD`. First failure again required about `-3.3m/s^2` while fallback applied `-4.0m/s^2`. | Do not use this run to judge the `3.5m` brake-margin experiment. Sync the updated config/code to the server and re-run; expected comparison baseline remains `20260621_200449`. |
| `20260621_215045` | Server validation of the brake-margin increase. | `yield_brake_distance_margin=3.5`; kept `yield_reference_decel=-3.5`, `yield_reference_min_speed=0.8`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate still failed only on solver failure: `var=0.066`, `fixed=0.070`. Safety/completion/yield order passed; no footprint collision. Compared with `20260621_200449`, headline solver fractions were effectively unchanged, though `var` hold failures reduced from 6 to 5. First failure remained step 26 `approach_yield_line`, `INF_OR_UNBD`, with distance-to-stop `4.23m`, required decel about `-3.31m/s^2`, and fallback `a_des=-4.0m/s^2`. | The 3.5m margin is not harmful but insufficient. Do not keep increasing margin blindly; the remaining bottleneck is still the mismatch between SMPC `A_MIN=-3.0` and yield/fallback braking near `-4.0`. Next improvement should align optimizer braking authority or make the reference enter the constrained problem slower before step 26. |
| Local pending after `20260621_215045` | Aligned SMPC braking authority with the yield/fallback controller. | `SMPC_MMPreds.A_MIN=-3.0 -> -4.0`; kept `yield_brake_distance_margin=3.5`, `yield_reference_decel=-3.5`, `yield_reference_min_speed=0.8`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Local syntax validation passed; Python/Gymnasium pre-CARLA gate passed `32/32` checks. Full local gate still cannot run the MATLAB phase because neither `matlab` nor `octave` is installed. Expected effect: reduce early `approach_yield_line` `INF_OR_UNBD` failures where first-failure debug required about `-3.31m/s^2` but the optimiser was limited to `-3.0m/s^2`. | Run CARLA and compare against `20260621_215045`; success means approach/hold solver failures drop without increasing collision risk, jerk, or visible harsh braking. |
| `20260621_221117` | Server validation of aligned SMPC braking authority. | `SMPC_MMPreds.A_MIN=-4.0`, `yield_brake_distance_margin=3.5`, `yield_reference_decel=-3.5`, `yield_reference_min_speed=0.8`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate still failed only on solver failure, but fixed-risk improved substantially: `fixed=0.055` versus `0.070` in `20260621_215045`; `var=0.066` stayed flat. Safety/completion/yield order passed; no footprint collision. Failure phases: fixed `9` approach / `3` hold, var `10` approach / `4` hold, all `INF_OR_UNBD`. First var failure still entered approach too fast (`v_curr=5.42m/s`, distance-to-stop `4.23m`, required decel `-3.48m/s^2`). | Keep `A_MIN=-4.0`; it helped fixed-risk and did not hurt safety. Next change should make the reference profile enter approach slower for var-risk, preferably a small step to `yield_reference_decel=-3.75` before trying `-4.0` again under the new `A_MIN`. |
| Local pending after `20260621_221117` | Modestly strengthened the yield reference deceleration to target var-risk approach speed. | `yield_reference_decel=-3.5 -> -3.75`; kept `SMPC_MMPreds.A_MIN=-4.0`, `yield_brake_distance_margin=3.5`, `yield_reference_min_speed=0.8`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | JSON and Python syntax validation passed; reference-profile simulation was smooth and bounded by `3.75m/s^2`; Python/Gymnasium pre-CARLA gate passed `32/32` checks. Full local gate still cannot run the MATLAB phase because neither `matlab` nor `octave` is installed. Expected effect: lower the pre-solve reference speed during early `approach_yield_line`, where var-risk first failure still entered at `v_curr=5.42m/s` and required `-3.48m/s^2`. | Run CARLA and compare against `20260621_221117`; success means var-risk solver failure drops below `0.066` and fixed-risk stays near or below `0.055` without worsening footprint safety or jerk. |
| `20260621_223615` | Server validation of modestly stronger yield reference decel. | `yield_reference_decel=-3.75`, `SMPC_MMPreds.A_MIN=-4.0`, `yield_brake_distance_margin=3.5`, `yield_reference_min_speed=0.8`, `yield_stop_buffer_distance=6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate still failed only on solver failure, but both policies are close to threshold: `var=0.055`, `fixed=0.060`. Safety/completion/yield order passed; no footprint collision. Compared with `20260621_221117`, var improved from `0.066` to `0.055`, fixed regressed from `0.055` to `0.060`, and longitudinal jerk stayed improved versus earlier high-jerk runs. First failure still showed local reference acceleration capped near `-3.0` despite the `-3.75` reference-decel target. | The direction helped var-risk but exposed that `RefTrajGenerator` still has `A_MIN=-3.0`. Next change should align the local reference generator braking bound with `SMPC_MMPreds.A_MIN=-4.0` instead of further changing stop geometry or activation distance. |
| Local pending after `20260621_223615` | Aligned the feasible local reference generator with the active SMPC acceleration limits. | `SMPCAgent` now passes the active acceleration bounds into `RefTrajGenerator`; during the early constructor call before `self.SMPC` exists, it uses cached expected bounds (`-4.0/2.0` for fixed/var SMPC, `-3.0/2.0` for open-loop/OBCA), then prefers `self.SMPC.A_MIN/A_MAX` after solver construction. This keeps `yield_reference_decel=-3.75`, `SMPC_MMPreds.A_MIN=-4.0`, `yield_brake_distance_margin=3.5`, stop buffer `6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | JSON and Python syntax validation passed; Python/Gymnasium pre-CARLA gate passed `32/32` checks. Full local gate still cannot run the MATLAB phase because neither `matlab` nor `octave` is installed. A first attempt that directly read `self.SMPC.A_MIN` crashed during setup because `reference_regeneration()` runs before solver construction; this hotfix removes that initialization-order bug. | Run CARLA and compare against `20260621_223615`; success means the scenario setup no longer crashes and both required policies move from about `0.055-0.060` to `<=0.050` without reducing center/footprint clearance or increasing jerk substantially. |
| `20260621_230902` | Server validation of reference-generator acceleration-bound alignment. | `RefTrajGenerator` used the active `A_MIN=-4.0` for both fixed/var SMPC, with `yield_reference_decel=-3.75`, `yield_brake_distance_margin=3.5`, `yield_reference_min_speed=0.8`, stop buffer `6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate failed only on solver failure: `fixed=0.056`, `var=0.117`. Safety/completion/yield order passed; no footprint collision. Compared with `20260621_223615`, fixed improved slightly (`0.060 -> 0.056`), but var regressed sharply (`0.055 -> 0.117`), with hold failures increasing to `17`. First-failure reference acceleration now starts near `-4.0`, confirming the alignment worked mechanically but is too aggressive for var-risk. | Reject global reference-generator `A_MIN=-4.0` for both policies. Next change should be policy-specific: keep fixed-risk reference generator at `-4.0`, but restore var-risk reference generator to the less aggressive `-3.0` while retaining `SMPC_MMPreds.A_MIN=-4.0` and `yield_reference_decel=-3.75`. |
| Local pending after `20260621_230902` | Made the feasible local reference generator policy-specific. | `smpc_var_risk` uses `RefTrajGenerator.A_MIN=-3.0`; `smpc_fixed_risk` uses `RefTrajGenerator.A_MIN=-4.0`; both keep `SMPC_MMPreds.A_MIN=-4.0`, `yield_reference_decel=-3.75`, `yield_brake_distance_margin=3.5`, `yield_reference_min_speed=0.8`, stop buffer `6.25`, activation/caution `12.0`, recovery `4.0/1.2`. `smpc_debug_setup.json` now records the reference-generator bounds. | JSON and Python syntax validation passed; Python/Gymnasium pre-CARLA gate passed `32/32` checks. Full local gate still cannot run the MATLAB phase because neither `matlab` nor `octave` is installed. Expected effect: recover the `20260621_223615` var-risk behaviour while preserving the slight fixed-risk improvement from `20260621_230902`. | Run CARLA and compare against both `20260621_223615` and `20260621_230902`; success means var-risk returns near `0.055` instead of `0.117`, fixed-risk stays near `0.056`, and neither policy loses footprint safety/yield order. |
| `20260622_223431` | Server validation of policy-specific reference-generator bounds. | `smpc_var_risk`: `RefTrajGenerator.A_MIN=-3.0`; `smpc_fixed_risk`: `RefTrajGenerator.A_MIN=-4.0`; both kept `SMPC_MMPreds.A_MIN=-4.0`, `yield_reference_decel=-3.75`, `yield_brake_distance_margin=3.5`, `yield_reference_min_speed=0.8`, stop buffer `6.25`, activation/caution `12.0`, recovery `4.0/1.2`. | Gate failed only on solver failure but recovered from the global-refgen regression: `var=0.060`, `fixed=0.055`. Safety/completion/yield order passed; no footprint collision. Failures were consecutive early rule-yield steps only: fixed `10` approach / `2` hold, var `11` approach / `2` hold, all `INF_OR_UNBD`. Center clearance stayed high (`var=6.411m`, `fixed=6.397m`) and longitudinal jerk was controlled (`var=3.24`, `fixed=3.56`). | Keep policy-specific reference-generator bounds; this is the best combined direction so far. Remaining failures are not recovery or collision failures, but solver attempts during rule-supervised approach/hold. Next improvement should make the rule-aware supervisor bypass or gate SMPC solving during deterministic yield-stop/hold phases, or otherwise stop counting intentionally overridden yield-hold steps as SMPC solve failures. |
| Local pending dissertation pivot | Added logging-only interaction severity signal for the dissertation adaptive-risk direction. | No control parameters changed. `SMPCAgent` now records `severity_score`, `severity_phase`, and `interaction_severity` factors derived from distance to conflict, TTC gap, priority state, overlap risk, and target-cleared state. Added `docs/architecture/Rule_Aware_Adaptive_Risk_SMPC_Dissertation_Plan.md`. | Python syntax validation passed and pre-CARLA give-way validation passed. No CARLA behaviour change is expected because the severity signal is currently logging-only. | Use the next CARLA run to inspect whether severity is high during approach/hold and drops after target clearance. Only after that should `adaptive_interaction_severity` change SMPC risk parameters. |
| `20260627_140331` | Server validation of logging-only interaction severity signal. | Same controller parameters as `20260622_223431`; no control behaviour change intended. Ran `smpc_var_risk` and `smpc_fixed_risk` with `risk_profile=upstream_code`. | Post-CARLA safety/yield passed but strict solver gate still failed: `var=0.0550`, `fixed=0.0550`, threshold `0.050`. No footprint collision; center clearance `var=6.415m`, `fixed=6.413m`; target cleared before ego entered conflict zone for both policies. Failures remained only in rule-yield phases: both policies `10` approach / `2` hold. Severity logging behaved as intended directionally: `free_drive` mean `0.019`, `approach_yield_line` mean `0.530`, `hold_yield_line` mean `0.509`, `released_recovery` mean `0.000`. | Logging validates the dissertation severity concept. The score is directional but not high enough to hit the `high` phase under current thresholds; next adaptive-risk implementation should rescale or phase-map approach/hold severity before using it to tighten chance constraints. Do not resume geometry-only tuning. |
| Local pending adaptive-risk implementation | Implemented `risk_profile=adaptive_interaction_severity`. | `run_all_scenarios.py` now accepts the adaptive profile. `SMPC_MMPreds` now supports a per-step risk target parameter: variable-risk updates the total target-probability lower bound; fixed-risk updates `mmrisk_std/mmrisk_prob` parameters. `SMPCAgent` maps rule-aware severity to risk: approach floor `0.72`, hold floor `0.78`, cautious floor `0.45`, observe floor `0.35`, high tightening `Phi^-1(0.98)=2.0537`, upstream nominal `1.64`, relaxed-after-clearance tightening `Phi^-1(0.90)=1.2816`. | Python syntax validation passed. CLI help accepts `adaptive_interaction_severity`. Pre-CARLA give-way validation passed. Local full solver import is not possible in `.venv-precarla` because CasADi is not installed; full validation must run on the server `carla_modern` environment. | Next server run should use `--risk_profile adaptive_interaction_severity` and compare against `20260627_140331`. Expected debug signature: approach/hold risk phase becomes medium/high with applied tightening around `1.94-1.96`, while released recovery uses relaxed tightening around `1.28`. |
| `20260627_143621` | Server validation of full adaptive-risk implementation. | Ran `smpc_var_risk`, `smpc_fixed_risk`, `notv`, and `notv_cl` with `risk_profile=adaptive_interaction_severity`. Adaptive mapping used approach floor `0.72`, hold floor `0.78`, relaxed-after-clearance `Phi^-1(0.90)`. | Safety/completion/yield order passed, but strict solver gate still failed and worsened versus the logging-only run: `var=0.0596`, `fixed=0.0602` versus `20260627_140331` `0.0550/0.0550`. Failures remained only in rule-yield phases: both policies `11` approach / `2` hold, all `INF_OR_UNBD`. Adaptive risk worked mechanically: approach tightening mean `1.938` (`target_prob=0.9737`), hold tightening mean `1.963` (`0.9752`), released recovery tightening `1.282` (`0.9000`). Center clearance stayed high (`var=6.363m`, `fixed=6.414m`), completion valid, and average longitudinal jerk stayed controlled (`var=3.105`, `fixed=3.224`). | The first adaptive mapping is too strict during the exact phases where the optimiser is already feasibility-limited. Do not keep approach/hold floors `0.72/0.78` as-is. Next change should reduce the tightening ramp or make adaptive risk relax feasibility when the rule supervisor is already deterministically yielding; a candidate is approach/hold floors around `0.35/0.45` or using adaptive risk primarily after prediction overlap becomes critical, while preserving relaxed-after-clearance. |
| Local pending adaptive-risk retune after `20260627_143621` | Reduced adaptive risk tightening during deterministic rule-yield phases. | `SMPCAgent._adaptive_risk_allocation`: approach floor `0.72 -> 0.35`, hold floor `0.78 -> 0.45`, cautious floor `0.45 -> 0.25`, observe floor `0.35 -> 0.20`. Added a mild tightening scale: for effective severity `<0.85`, use only `0.35 * effective_score` of the upstream-to-paper tightening span; critical interactions `>=0.85` can still approach paper-style tightening. Relaxed-after-clearance remains `Phi^-1(0.90)=1.2816`. | Python syntax validation passed, `git diff --check` passed, and pre-CARLA give-way validation passed. Based on `20260627_143621` severity values, expected approach/hold applied tightening should drop from about `1.94/1.96` to about `1.71-1.72`, preserving adaptive ordering while reducing `INF_OR_UNBD` pressure. | Run CARLA with the same command as `20260627_143621` and compare first against the logging-only baseline `20260627_140331` and the strict adaptive run `20260627_143621`. Success means solver failure returns near or below `0.055` while adaptive debug still shows higher risk during approach/hold than released recovery. |
| `20260627_150047` | Server validation of the milder adaptive-risk retune. | Ran `smpc_var_risk`, `smpc_fixed_risk`, `notv`, and `notv_cl` with `risk_profile=adaptive_interaction_severity`. Adaptive mapping used approach floor `0.35`, hold floor `0.45`, mild scale `0.35 * effective_score`, relaxed-after-clearance `Phi^-1(0.90)`. | Safety/completion/yield order passed, but strict solver gate still failed: `var=0.0605`, `fixed=0.0514`. This is a strong improvement for fixed-risk versus strict adaptive (`0.0602 -> 0.0514`) and better than `20260622_223431` fixed (`0.0553`), but var-risk remained around the same level (`0.0596 -> 0.0605`). Failure phases: fixed `9` approach / `2` hold; var `10` approach / `3` hold, all `INF_OR_UNBD`. Adaptive risk now applied the intended milder tightening: approach mean `1.717` (`target_prob=0.9570`), hold mean `1.714` (`0.9567`), released recovery `1.282` (`0.9000`). Center clearance stayed high (`var=6.382m`, `fixed=6.405m`) and completion valid; completion time improved slightly (`var=10.70s`, `fixed=10.65s`). | Milder adaptive risk helps fixed-risk and preserves safety/efficiency, but var-risk remains feasibility-limited. Next change should be policy-specific: keep the milder map for fixed-risk, but make var-risk closer to upstream during deterministic approach/hold, or bypass/gate SMPC solves during rule-supervised yield phases. Do not return to strict `0.72/0.78` floors. |
| Local pending policy-specific adaptive-risk map after `20260627_150047` | Made adaptive risk allocation policy-specific. | `fixed_risk` keeps the successful mild map from `20260627_150047`: approach/hold floors `0.35/0.45`, scale `0.35 * effective_score`. `var_risk` now uses a feasibility-preserving map during deterministic yield: approach/hold floors `0.20/0.25`, cautious/observe floors `0.15/0.10`, scale `0.12 * effective_score`. Critical severity `>=0.85` can still use strong tightening for both policies, and target-cleared/recovery still relaxes to `Phi^-1(0.90)`. | Python syntax validation passed, `git diff --check` passed, and pre-CARLA give-way validation passed. Expected applied tightening for the `20260627_150047` var-risk severity values drops from about `1.717/1.714` to about `1.666`, close to upstream `1.64`, while fixed-risk remains at the previously beneficial `1.71-1.72`. | Run CARLA with the same adaptive command. Success means fixed-risk remains near `0.051` or improves, while var-risk returns toward the upstream/logging-only level or below without losing adaptive debug evidence. |
| `20260627_152103` | Server validation of the policy-specific adaptive-risk map. | Ran `smpc_var_risk`, `smpc_fixed_risk`, `notv`, and `notv_cl` with `risk_profile=adaptive_interaction_severity`. `fixed_risk` used the mild map (`tight≈1.717/1.714` in approach/hold); `var_risk` used feasibility-preserving map (`tight≈1.666/1.668`, near upstream `1.64`). | Safety/completion/yield order passed, but solver gate regressed: `fixed=0.0556`, `var=0.0935`. Fixed returned close to the earlier non-adaptive baseline rather than keeping the `20260627_150047` improvement. Var-risk worsened badly, with failures shifting to `7` approach / `13` hold, all `INF_OR_UNBD`; center clearance dropped but stayed safe (`var=5.564m`, `fixed=6.415m`), and var longitudinal jerk worsened (`5.378`). Adaptive risk was applied as intended mechanically: `var_risk` policy map logged `var_feasibility_preserving`, target probability about `0.952`, recovery relaxed to `0.900`. | Reject the policy-specific near-upstream var-risk map. Making var-risk looser during deterministic yield does not solve feasibility and worsens hold behavior. The best adaptive candidate remains the unified mild map from `20260627_150047` for fixed-risk, but var-risk needs a different architectural treatment: bypass/gate SMPC solves during rule-supervised stop/hold, or keep adaptive risk only as a logged/analysis variable for var-risk while deterministic yield controller handles the stop. |
| Local pending rule-yield solve bypass after `20260627_152103` | Added deterministic rule-yield SMPC solve bypass for the adaptive profile. | Rejected the failed `var_feasibility_preserving` map and restored the unified mild adaptive map (`approach/hold floors 0.35/0.45`, scale `0.35 * effective_score`). Added `_should_bypass_smpc_for_rule_yield`: when `risk_profile=adaptive_interaction_severity`, non-open-loop/non-OBCA policy, valid priority prediction, active `approach_yield_line` or `hold_yield_line`, the controller skips the SMPC solve and directly applies the rule-aware yield controller. The debug log records `solver_bypass.enabled=true`, `reason=deterministic_rule_yield_control`; `is_opt=true` because no SMPC solve was attempted. | Python syntax validation passed, `git diff --check` passed, and pre-CARLA give-way validation passed. Expected effect: deterministic yield frames should stop contributing `INF_OR_UNBD` solver failures, while safety/yield order remains governed by the existing rule controller. This is an architectural correction: these frames are rule-supervised stop/hold actions, not meaningful SMPC feasibility tests. | Run CARLA with the same adaptive command. Success means `solver_failure_frac` should drop sharply for both `smpc_var_risk` and `smpc_fixed_risk`; debug should show bypass counts roughly matching prior approach/hold failure windows. |
| `20260627_155115` | Server validation of rule-yield solve bypass. | Ran `smpc_var_risk`, `smpc_fixed_risk`, `notv`, and `notv_cl` with `risk_profile=adaptive_interaction_severity`. Unified mild adaptive risk was active, and deterministic `approach_yield_line` / `hold_yield_line` frames were bypassed instead of solved by SMPC. | Required post-CARLA gate passed for both SMPC policies: `var=0.000`, `fixed=0.000` solver failure; completion/yield order passed; no footprint collision. Bypass worked exactly where expected: both policies bypassed `51` frames (`17` approach / `34` hold), and those frames previously dominated `INF_OR_UNBD` failures. Adaptive risk remained meaningful in logs: approach tightening mean `1.720` (`target_prob=0.9573`), hold `1.723` (`0.9576`), recovery `1.282` (`0.9000`). Completion improved (`var=10.40s`, `fixed=10.45s`) and solve time dropped, but clearance decreased (`var=5.215m`, `fixed=5.340m`) and longitudinal jerk increased (`var=4.444`, `fixed=4.544`) versus prior high-clearance runs. | This is the first full pass of the strict post-CARLA gate and is the strongest dissertation candidate so far. Keep the bypass architecture, but next evaluate video quality and consider comfort smoothing only if needed; do not undo bypass to chase pure SMPC feasibility. |
| Local pending ego left-turn-lane start fix after `20260627_155115` | Adjusted ego initial lateral offset based on video inspection. | In `scenario_uk_give_way.json`, changed the ego `start_left_offset` from `+1.85` to `-1.85`. Target offset remains `+1.85`; ego `goal_left_offset` remains `+1.85` to preserve the validated exit route and conflict-zone timing. | JSON validation, Python syntax validation, `git diff --check`, and pre-CARLA give-way validation passed. The reason for this change is visual correctness: the `20260627_155115` video behaviour was good, but the ego initially appeared between approach lanes rather than in the visual left-turn lane. This is a scenario-geometry fix, not a controller/risk-allocation change. Pre-CARLA timing remains meaningful: target reaches conflict first (`ego_minus_target_ttc=0.94s`), no-yield creates footprint conflict, and give-way avoids footprint overlap. | Re-run the same adaptive-risk experiment and inspect the first seconds of the video. Success means the ego starts in the left-turn lane while preserving completion, yield order, no footprint collision, and low solver failure. |
| `20260627_161450` | Server validation after ego start-lane offset fix. | Ran the same adaptive-risk command after changing ego `start_left_offset` from `+1.85` to `-1.85`. Target and ego goal offsets were unchanged. | Result is mixed and does not replace the `20260627_155115` milestone. `smpc_var_risk` improved quantitatively and passed: completion valid at `9.95s`, no footprint collision, yield order passed, center clearance `8.001m`, `solver_failure_frac=0.000`, bypass `46` deterministic yield frames (`35` approach / `11` hold). `smpc_fixed_risk` failed the strict gate: completion invalid / ran to `29.95s`, `solver_failure_frac=0.090`, `54` `INF_OR_UNBD` failures all in `free_drive`, no bypass triggered, high route deviation (`hausdorff_dist_notv=25.26`, `max_abs_ey_debug=10.15`). Safety and yield ordering were still okay for fixed-risk (`center_dmin=6.236m`, no footprint collision, target first), but it drifted/failed to complete after the geometry change. | The negative ego start offset likely fixed the visual lane placement for var-risk but exposed a fixed-risk route/reference issue caused by mixing `ego start=-1.85` with `ego goal=+1.85`. Do not promote this run as the dissertation milestone. Next change should make the ego start and exit lane geometry internally consistent or apply the visual lane correction through a route-aware start transform rather than a raw sign flip. |
| Local revert of ego start offset after user image inspection | Reverted the over-corrected ego start offset. | User screenshots showed the `-1.85` trial placed the ego too far right near the road edge; the desired red-rectangle position is one lane left of that. Changed ego `start_left_offset` back from `-1.85` to `+1.85`, matching the lane-centre-scale geometry used by the successful `20260627_155115` milestone. | Local validation pending. This is a visual start-position correction and restores the geometry that passed both SMPC policies in `20260627_155115`. | Re-run CARLA and inspect only the first seconds first. If the EV appears at the red-rectangle lane-centre position and the gate remains like `20260627_155115`, keep this as the final visual geometry. |
| `20260627_163523` | Server validation after restoring ego start offset to the user-marked lane-centre position. | Ran the same adaptive-risk command after changing ego `start_left_offset` back to `+1.85`. This restores the visual lane-centre-scale geometry from the successful `20260627_155115` milestone and rejects the over-corrected `-1.85` trial. | Required post-CARLA gate passed for both SMPC policies. `smpc_fixed_risk`: completion valid at `10.35s`, no footprint collision, yield order passed, center clearance `5.308m`, `solver_failure_frac=0.000`, bypass `51` frames (`18` approach / `33` hold). `smpc_var_risk`: completion valid at `10.45s`, no footprint collision, yield order passed, center clearance `5.342m`, `solver_failure_frac=0.000`, bypass `51` frames (`17` approach / `34` hold). Adaptive risk behaved as intended: approach/hold tightening stayed near `1.72`, recovery relaxed to `1.282`. Compared with `20260627_155115`, metrics are effectively equivalent: zero solver failures, similar completion time, clearance, jerk, and route deviation. | This is now the best post-screenshot validation run. If visual inspection confirms the ego starts at the red-rectangle lane-centre position, promote `20260627_163523` over `20260627_155115` as the final milestone because it preserves the full gate pass while using the corrected/confirmed visual start geometry. |
| Local pending fine ego start left-shift after `20260627_163523` | Fine-tuned the ego start offset based on the latest screenshot. | User screenshot showed `+1.85` is in the correct direction but still slightly right of the red rectangle. Changed ego `start_left_offset` from `+1.85` to `+2.75`, a conservative additional left shift of `0.90m`. Target offset and ego goal offset remain `+1.85` to preserve the validated target lane, exit route, and conflict-zone timing. | JSON validation, Python syntax validation, `git diff --check`, and pre-CARLA give-way validation passed. The no-yield rollout still creates footprint conflict, and the simple give-way rollout still avoids overlap. This is a visual start-position micro-adjustment, not a controller or risk-allocation change. | Re-run the same adaptive-risk batch. Success means the first video seconds place the ego inside the red rectangle while preserving the `20260627_163523` gate behaviour: both SMPC policies complete, no collision, target-first yield order, and `solver_failure_frac=0.000`. |
| `20260627_170746` | Server run with `ego start_left_offset=+2.75`, but using upstream risk profile. | Ran `smpc_var_risk`, `smpc_fixed_risk`, `smpc_open_loop`, `notv`, and `notv_cl` with `risk_profile=upstream_code` rather than `adaptive_interaction_severity`. This run includes the `+2.75` visual start offset and generated `paper_panel`, so it is useful as a baseline/panel run, but it does not validate the final adaptive-risk method. | Strict required-policy gate failed because adaptive solve bypass was disabled by the upstream profile. `smpc_fixed_risk`: completion valid but `solver_failure_frac=0.221`, failures `12` approach / `35` hold / `13` recovery, all `INF_OR_UNBD`, center clearance `4.686m`. `smpc_var_risk`: completion valid but `solver_failure_frac=0.209`, failures `15` approach / `31` hold / `15` recovery, all `INF_OR_UNBD`, center clearance `4.463m`. `smpc_open_loop` ran and produced the paper panel, but violated yield order (`ego_enter=142.62s` before `target_exit=146.37s`) while avoiding collision. | Do not treat this as a failure of the `+2.75` visual geometry or of the proposed method. It is mostly an upstream-risk baseline demonstrating why adaptive rule-yield bypass is needed. Re-run with `--risk_profile adaptive_interaction_severity` to validate whether `+2.75` can become the final visual-geometry milestone. |
| `20260627_194959` | Server validation of `ego start_left_offset=+2.75` with the final adaptive-risk profile. | Ran `smpc_var_risk`, `smpc_fixed_risk`, `notv`, and `notv_cl` with `risk_profile=adaptive_interaction_severity`. Scenario geometry used `ego start_left_offset=+2.75`, `ego goal_left_offset=+1.85`, and target offsets `+1.85`. Unified mild adaptive risk and deterministic `approach_yield_line` / `hold_yield_line` bypass were active. | Safety, completion, and yield order are good, but the strict required-policy gate narrowly failed. `smpc_var_risk` passed with `solver_failure_frac=0.04464`, completion valid, no footprint collision, target cleared before ego entered, center clearance `4.237m`. `smpc_fixed_risk` failed only because `solver_failure_frac=0.05172` exceeds the `0.050` gate by `0.00172`; completion valid, no footprint collision, target cleared before ego entered, center clearance `4.234m`. All failures occurred in `released_recovery` (`fixed=12`, `var=10`, all `INF_OR_UNBD`). Bypass worked correctly in deterministic yield phases: both policies bypassed `51` frames (`11` approach / `40` hold). Adaptive risk was applied as intended: approach/hold tightening mean about `1.72` with target probability about `0.957`, while released recovery relaxed to `Phi^-1(0.90)=1.282`. | This validates the adaptive-risk mechanism under the `+2.75` visual geometry, but it is not yet the final milestone because fixed-risk misses the solver gate narrowly. Do not change approach/hold risk floors or undo `+2.75` solely from this result. If the video visually matches the red rectangle, next fix should target early `released_recovery` infeasibility, for example by extending deterministic bypass or recovery handoff for the first few low-speed recovery frames after target clearance. |
| Local pending recovery-handoff bypass after `20260627_194959` | Kept the confirmed `+2.75` visual geometry and targeted only early `released_recovery` solver infeasibility. | In `smpc_agent.py`, replaced the simple boolean bypass helper with a reasoned bypass decision. The adaptive profile still bypasses deterministic `approach_yield_line` / `hold_yield_line`, and now additionally bypasses only the first low-speed released-recovery handoff frames after the priority target has cleared the conflict zone. Conditions: `risk_profile=adaptive_interaction_severity`, non-open-loop/non-OBCA, valid priority prediction, `target_cleared_conflict=true`, `speed <= max(2.0, 0.5 * yield_recovery_speed)`, and either the first release frame after hold or the first `min(15, ceil(0.25 * yield_recovery_steps))` recovery frames. Debug now records `solver_bypass.reason=deterministic_rule_yield_recovery_handoff` and `recovery_steps_remaining`. | Python syntax validation and `git diff --check` passed. Replay against the `20260627_194959` debug window shows the new condition would cover all old recovery failures: fixed-risk failures `[69-78, 80, 81]` and var-risk failures `[69-77, 79]`, while only adding bypass over the early handoff window `[69-84]`. This should bring fixed-risk below the `0.050` gate without changing conflict-zone logic, adaptive risk floors, or the visually confirmed start geometry. | Re-run the adaptive batch with the same `+2.75` geometry. Success means both SMPC policies pass completion, no footprint collision, target-first yield order, and fixed-risk `solver_failure_frac` drops below `0.050` with debug showing recovery handoff bypass only in the early released-recovery window. |
| `20260627_201840` | Server validation after early `released_recovery` handoff bypass. | Ran the same final adaptive command: `smpc_var_risk`, `smpc_fixed_risk`, `notv`, and `notv_cl` with `risk_profile=adaptive_interaction_severity`, `solver_backend=gurobi`, and confirmed `ego start_left_offset=+2.75`. The new recovery-handoff bypass was active. | Required policies now pass the strict post-CARLA gate. `smpc_fixed_risk`: completion valid at `11.10s`, no footprint collision, target cleared before ego entered, center clearance `4.227m`, `solver_failure_frac=0.000`. `smpc_var_risk`: completion valid at `11.10s`, no footprint collision, target cleared before ego entered, center clearance `4.147m`, `solver_failure_frac=0.000`. Debug confirms the intended bypass distribution for both policies: `51` deterministic approach/hold frames (`11` approach / `40` hold) plus exactly `16` early `released_recovery` handoff frames (`steps 69-84`) with reason `deterministic_rule_yield_recovery_handoff`. Adaptive risk remains interpretable: approach/hold tightening mean about `1.72` with target probability about `0.957`, recovery relaxed to `Phi^-1(0.90)=1.282`. | This is the best current final-method run with the user-confirmed visual geometry. Promote it as the current dissertation candidate for the proposed method. The trade-off is reduced clearance versus the older `+1.85` zero-failure milestone (`~4.15-4.23m` vs `~5.31-5.34m`) and moderate route deviation (`completion_ey≈-3.6`, still completion-valid). Do not add broader recovery bypass; the current handoff window is sufficient and bounded. Next work should focus on generating comparable baselines/paper panels and documenting the method, not further solver-failure tuning. |
| `20260627_203901` | Repeat adaptive run with `+2.75` geometry, but without `smpc_open_loop`. | Ran `smpc_var_risk`, `smpc_fixed_risk`, `notv`, and `notv_cl` with `risk_profile=adaptive_interaction_severity`; this was not the full final dissertation batch because `smpc_open_loop` was omitted, so paper-panel postprocess again failed with `Missing required policies for panel plot: ['smpc_open_loop']`. Geometry remained correct: ego `start_left_offset=+2.75`, ego goal `+1.85`, target offsets `+1.85`. | Solver health remained fixed (`solver_failure_frac=0.000` for both policies) and bypass stayed bounded (`51` approach/hold frames plus `16` early recovery frames for both policies), but strict post-CARLA safety failed for `smpc_fixed_risk`: center clearance `3.689m`, footprint collision `0.15s`, min footprint separation `0.0m`. `smpc_var_risk` still passed with center clearance `4.236m` and no footprint collision. Yield order still passed for fixed-risk (`target_exit=2301.75s`, `ego_enter=2302.90s`), so the failure is not a priority-order violation. The footprint overlap occurs just after the target leaves the conflict-radius zone, before the ego is counted as entering that zone; this means the current `target_cleared_conflict` / release condition is too optimistic for vehicle footprint safety. | Do not promote this run. Keep `20260627_201840` as the best passed candidate, but treat `20260627_203901` as evidence that the final method needs a robustness fix before dissertation freeze. Next code change should add a small target-clearance release buffer, e.g. require the priority target to be beyond `yield_conflict_radius + release_clearance_margin` before starting `released_recovery`, rather than changing solver/risk floors or widening recovery bypass. After that, rerun the full dissertation batch including `smpc_open_loop`. |
| Local pending release-clearance buffer after `20260627_203901` | Targeted the remaining footprint-safety robustness issue while preserving the confirmed geometry and solver-stability fixes. | Added `yield_release_clearance_margin=1.0m` to `SMPCAgent`, `VehicleParams`, and `give_way_smpc_tuning.json`. The state machine now distinguishes nominal conflict-radius clearance from release clearance: `target_nominally_cleared_conflict` records `target_distance_to_conflict < -yield_conflict_radius`, while `target_cleared_conflict` used for release requires `target_distance_to_conflict < -(yield_conflict_radius + yield_release_clearance_margin)`. Candidate priority selection uses the same buffered threshold so the target remains the active priority target while it is inside the release buffer. Debug logs now include `target_release_clearance_distance` and `target_release_clearance_margin`. | This should delay release by roughly `1.0m / target_speed`, about `0.17s` at the observed `5.7-5.8m/s` target speed. That directly covers the `0.15s` fixed-risk footprint overlap from `20260627_203901`, without changing `+2.75` visual geometry, risk floors, stop-line distance, or the bounded 16-frame recovery handoff. | Re-run the full dissertation batch including `smpc_open_loop`. Success requires both SMPC policies to pass footprint collision replay, yield order, completion, and `solver_failure_frac=0.000`, while paper-panel postprocess should also succeed because `smpc_open_loop` is included. |
| `20260627_205856_final_dissertation` | Full dissertation batch after `yield_release_clearance_margin=1.0m`, including `smpc_open_loop`. | Ran `notv`, `notv_cl`, `smpc_var_risk`, `smpc_fixed_risk`, and `smpc_open_loop` with `risk_profile=adaptive_interaction_severity`. Paper-panel postprocess succeeded and generated `paper_panel.png/svg`, so the missing-baseline issue is fixed. `yield_release_clearance_margin=1.0m` was active in debug setup and delayed fixed-risk recovery from the nominal-clearance step to `step 75` (`target_distance_to_conflict=-5.25m`). | Gate still failed because fixed-risk collided during `hold_yield_line`, before recovery release. `smpc_fixed_risk`: `solver_failure_frac=0.000`, completion valid, yield order valid, but footprint collision `0.80s`, center clearance `3.315m`, min footprint separation `0.0m`. Collision started at gate step `61`, while debug shows fixed-risk stayed in `hold_yield_line` through step `74` and entered `released_recovery` at step `75`. Therefore the 1.0m release buffer worked but addressed the wrong part of the failure. `smpc_var_risk` passed with `center_dmin=3.804m` but min footprint separation was only `0.047m`, so it is also close to the safety boundary. `smpc_open_loop` passed safety but had poor trajectory quality and very large route deviation (`hausdorff_dist_notv=28.82`, `max_abs_ey_debug=21.12`). | Do not promote this run. The remaining issue is the waiting pose / stop-line clearance, not solver feasibility, risk allocation, or release timing. Next change should move the yield stop line upstream by increasing `yield_stop_buffer_distance` from `6.25m` to about `8.0m` (`+1.75m`) while keeping `+2.75` start geometry, adaptive risk, release buffer, and bounded recovery bypass unchanged. This should increase hold-phase footprint clearance by moving the ego center away before the target passes. |
| Local pending hold-line clearance fix after `20260627_205856_final_dissertation` | Targeted fixed-risk footprint collision that occurred during `hold_yield_line`, before release. | Increased `yield_stop_buffer_distance` from `6.25m` to `8.0m` (`+1.75m`) in `SMPCAgent` defaults, `VehicleParams`, and `give_way_smpc_tuning.json`. Kept `ego start_left_offset=+2.75`, `yield_release_clearance_margin=1.0m`, adaptive risk allocation, deterministic approach/hold bypass, and bounded 16-frame recovery handoff unchanged. | Expected effect: move the ego waiting pose upstream by about `1.75m`, directly increasing footprint clearance while the target passes. This targets the `20260627_205856_final_dissertation` failure where fixed-risk collided for `0.80s` during hold (`center_dmin=3.315m`) before `released_recovery` began. Main risk: too large a stop buffer may look more conservative or slightly increase completion time; this is preferable to a hold-phase footprint collision and should be checked in video. | Re-run `run_give_way_final_dissertation_batch.sh`. Success requires both SMPC policies to pass footprint collision replay, yield order, completion, and `solver_failure_frac=0.000`; paper panel should remain generated because `smpc_open_loop` is included. |
| `20260627_212618_final_dissertation` | Full dissertation batch after `yield_stop_buffer_distance=8.0m` hold-line clearance fix. | Ran `notv`, `notv_cl`, `smpc_var_risk`, `smpc_fixed_risk`, and `smpc_open_loop` with `risk_profile=adaptive_interaction_severity`. Confirmed active settings: `ego start_left_offset=+2.75`, `yield_stop_buffer_distance=8.0m`, `yield_release_clearance_margin=1.0m`, adaptive risk, deterministic approach/hold bypass, and bounded recovery handoff. Paper-panel postprocess succeeded. | Required SMPC policies passed. `smpc_fixed_risk`: PASS, completion valid, `solver_failure_frac=0.000`, no footprint collision, center clearance `4.142m`, min footprint separation `0.347m`, target cleared before ego entered, completion time `11.60s`. `smpc_var_risk`: PASS, completion valid, `solver_failure_frac=0.000`, no footprint collision, center clearance `3.897m`, min footprint separation `0.135m`, target cleared before ego entered, completion time `12.55s`. Bypass remained bounded: fixed-risk `55` approach/hold frames plus `16` recovery-handoff frames; var-risk `56` approach/hold plus `16` recovery-handoff. `smpc_open_loop` is correctly non-required and produced the useful contrast: no collision but violated yield order (`ego_enter=4046.73s` before `target_exit=4050.48s`) with poor route quality. Overall gate is `WARN` only because open-loop is non-required and rule-violating. | Promote this as the current best full dissertation-batch candidate. It validates the full proposed method with required SMPC policies passing, paper panel generated, and open-loop showing the intended rule-awareness contrast. Caveat: var-risk safety margin is still tight (`min_footprint_separation=0.135m`), so do not reduce `yield_stop_buffer_distance` below `8.0m`. Next work should focus on writing results and producing tables/figures, not more solver tuning unless video review reveals unacceptable conservatism. |
| `20260628_103325_final_dissertation` | Repeat full final-method batch pulled with direct `scp -r` after the password-prompt issue was avoided via `SSH_ASKPASS`. | Ran the same final-method command as `20260627_212618_final_dissertation`: `notv`, `notv_cl`, `smpc_var_risk`, `smpc_fixed_risk`, and `smpc_open_loop` with `risk_profile=adaptive_interaction_severity`. This is not the no-adaptive-risk ablation; it is a repeat validation of the full proposed method. Active tuning remained `yield_stop_buffer_distance=8.0m`, `yield_release_clearance_margin=1.0m`, and final visual geometry. Paper panel and metrics were generated. | Required SMPC policies passed again. `smpc_fixed_risk`: PASS, completion valid, `solver_failure_frac=0.000`, no footprint collision, center clearance `4.137m`, min footprint separation `0.341m`, target cleared before ego entered, completion time `11.55s`. `smpc_var_risk`: PASS, completion valid, `solver_failure_frac=0.000`, no footprint collision, center clearance `4.060m`, min footprint separation `0.267m`, target cleared before ego entered, completion time `11.55s`. `smpc_open_loop` remains the intended non-required warning: no collision but violates yield order (`ego_enter=129.00s` before `target_exit=132.65s`) with large route deviation. Compared with `20260627_212618`, fixed-risk is effectively unchanged and var-risk footprint separation improved from `0.135m` to `0.267m`. | Treat this as repeatability evidence for the full method, not as the ablation. It strengthens the case that the current final configuration is stable enough to expand experiments. Next required experiment is still the no-adaptive-risk ablation using `rule_aware_static_risk`; do not rerun the same final-method batch again unless checking stochastic repeatability over multiple seeds. |
| Local pending no-adaptive-risk ablation setup | Prepared the next dissertation ablation to isolate the contribution of interaction-severity adaptive risk allocation. | Added `risk_profile=rule_aware_static_risk`, which uses static upstream risk (`tight=1.64`) but keeps the rule-aware deterministic bypass active. Updated `mpc_utils.py`, `smpc_agent.py`, `run_all_scenarios.py`, and `run_intersection_scenario.py`. Added `run_give_way_no_adaptive_risk_ablation.sh`, which runs the same full dissertation batch as the final method but replaces `adaptive_interaction_severity` with `rule_aware_static_risk`. | This avoids the confound in old `upstream_code` runs, where both adaptive risk and deterministic bypass were disabled. The new ablation keeps final geometry, `yield_stop_buffer_distance=8.0m`, `yield_release_clearance_margin=1.0m`, rule-aware supervisor, and bounded recovery handoff unchanged. Any difference from `20260627_212618_final_dissertation` should therefore be attributable mainly to disabling adaptive risk allocation. | Run `bash run_give_way_no_adaptive_risk_ablation.sh` on the server, then pull and compare against `20260627_212618_final_dissertation`. Key comparison: safety margins, yield order, completion time, comfort, solver failures, and whether static risk loses the interpretable phase-dependent tightening/relaxation. |
| Local pending target-speed controlled sweep script | Prepared the next full-experiment expansion after the no-adaptive-risk ablation. | Added `core/scripts/carla/run_give_way_target_speed_sweep.sh`. The script generates temporary scenario JSON files under `scenarios/generated/<timestamp>_target_speed_sweep/` and varies only the priority target vehicle `init_speed` / `nominal_speed`, defaulting to `4.5`, `6.0`, and `7.5m/s`. It keeps final geometry (`ego start_left_offset=+2.75`), `yield_stop_buffer_distance=8.0m`, `yield_release_clearance_margin=1.0m`, rule-aware supervisor, deterministic yield bypass, and bounded recovery handoff unchanged. For each target speed it runs the full adaptive method (`risk_profile=adaptive_interaction_severity`, policies `smpc_var_risk`, `smpc_fixed_risk`, `smpc_open_loop`) and the no-adaptive-risk ablation (`risk_profile=rule_aware_static_risk`, policies `smpc_var_risk`, `smpc_fixed_risk`). | Local validation: `bash -n` passed and the script is executable. It automatically runs `postcarla_trajectory_gate.py` after each batch and writes `target_speed_sweep_manifest.csv`, `target_speed_sweep_summary.csv`, and `target_speed_sweep_summary.md` in the sweep results root. Camera video is disabled by default for speed (`ENABLE_CAMERA_VIZ=0`) and can be enabled with `ENABLE_CAMERA_VIZ=1`. | Run this on the CARLA server after activating `carla_modern` and Gurobi. This is the recommended next experiment to move from single-case validation toward a controlled full experiment. The main claim to test is whether adaptive risk remains safe and route-stable as interaction severity changes with target speed, compared with rule-aware static risk and open-loop. |
| `20260628_155621_target_speed_sweep` | First controlled target-speed sweep for the full-experiment expansion. | Ran target speeds `4.5`, `6.0`, and `7.5m/s`. For each speed, ran adaptive full method (`adaptive_interaction_severity`: `smpc_var_risk`, `smpc_fixed_risk`, `smpc_open_loop`) and rule-aware static-risk ablation (`rule_aware_static_risk`: `smpc_var_risk`, `smpc_fixed_risk`). Camera video was disabled. Final geometry and yield tuning were unchanged. | All required SMPC policies passed the post-CARLA gate across all speeds and both risk profiles: no footprint collision, valid completion, valid yield order, and `solver_failure_frac=0.000`. Adaptive var-risk center clearance: `4.152m` at `4.5m/s`, `4.658m` at `6.0m/s`, `4.204m` at `7.5m/s`. Static var-risk center clearance: `4.045m`, `4.782m`, `4.152m`. Adaptive fixed-risk center clearance: `4.058m`, `4.203m`, `4.562m`. Static fixed-risk center clearance: `4.174m`, `4.856m`, `4.740m`. Open-loop remains the intended non-required contrast: collision-free but yield-order failure at every tested speed. Adaptive debug confirms phase-dependent risk remained active (`adaptive_frac=1.0`, `tight≈1.282-1.727`); static-risk runs kept fixed `tight=1.64` and `adaptive_frac=0.0`. | This is positive full-experiment evidence: the rule-aware method is robust to controlled target-speed changes. The result does not show adaptive risk dominating static risk on raw clearance; static often gives larger clearance and can be more conservative/slower. The dissertation claim should be: both rule-aware profiles are safe, while adaptive risk provides an interpretable phase-dependent risk mechanism and sometimes improves efficiency/route stability. Script issue found: because `INCLUDE_NOTV=0`, `compute_scenario_results.py` could not generate `df_final.csv`, so the original sweep summary was empty. A local reconstructed summary was generated from gate/debug files at `target_speed_sweep_gate_debug_summary.md/csv`, and the script default was fixed to `INCLUDE_NOTV=1` for future runs. |
| Local completion-alignment fix for post-turn tilted EV body | Fixed the visual issue where the EV could finish the rollout while still slightly diagonal after the left turn. Analysis showed this was not an adaptive-risk failure: previous completion occurred with `s_to_end≈7m`, `goal_dist≈7.9m`, `completion_ey≈-3.1m`, and nonzero heading error, because completion used loose hard-coded thresholds and no heading-alignment condition. | Moved completion thresholds into the tuned ego config and tightened them: `completion_s_margin: 6.0 -> 2.0`, `completion_goal_dist: 8.0 -> 4.0`, `completion_lateral_error: 4.0 -> 1.5`, and added `completion_heading_error=0.1rad` (about `5.7deg`). Extended the tuned ego downstream goal by setting `goal_longitudinal_offset: 20.0 -> 30.0` through `give_way_smpc_tuning.json`, so the route has a longer straight segment after the turn. Updated `SMPCAgent` completion logic to require both lateral and heading alignment, and updated `compute_scenario_results.py` plus `postcarla_trajectory_gate.py` so metrics/gates use the stricter `heading_ok` condition. | Local validation only: tuning JSON is valid, affected Python files compile, and the scenario tuning injection resolves the new ego values correctly (`goal_longitudinal_offset=30.0`, `completion_s_margin=2.0`, `completion_goal_dist=4.0`, `completion_lateral_error=1.5`, `completion_heading_error=0.1`). | Next CARLA run should verify visual alignment. Expected effect: rollouts continue several more meters after the left turn, so the EV should be aligned with the target lane before completion. Possible side effect: completion time will increase and some previous PASS runs may need revalidation under the stricter completion gate; safety/yield logic is unchanged. |
| `20260628_231246_completion_alignment_check` | First CARLA validation after tightening completion alignment. | Ran the requested alignment-check command with `smpc_var_risk`, `smpc_fixed_risk`, `smpc_open_loop`, `with_notv`, `with_notv_cl`, and `risk_profile=adaptive_interaction_severity`. | Batch did not complete. `notv` and `notv_cl` succeeded and generated videos. The first SMPC subrun (`smpc_var_risk`) ran until step `293` but crashed before writing `scenario_result.pkl`, so fixed-risk/open-loop were not executed and post-CARLA gate only reported non-required `notv/notv_cl` warnings. The new completion condition worked as intended at the failure point: `goal_dist=3.196m`, `s_to_end=2.0m`, `ey=0.073m`, `lateral_ok=True`, but `epsi=0.313rad` and `heading_ok=False`, so the rollout correctly did not terminate while the vehicle body was still angled. The crash was a separate route-end reference issue: `SMPC.update()` expected `a_ref/df_ref` length `N+1=11`, but near the extended route end `a_ref/df_ref` had length `10`, causing CasADi `Dimension mismatch. lhs is 1-by-11, while rhs is [10,1]`. | Added a local fix in `SMPCAgent`: `_horizon_slice_with_tail_padding()` pads state/input horizon slices to fixed `N+1` length by repeating the last available reference at route end. `x/y/psi/v_ref`, `a/df_ref`, and local linearization slices now use this fixed-length padding. Python compile validation passed. Re-run the same completion-alignment check; expected result is no CasADi dimension mismatch and the rollout should continue until `heading_ok=True`. |
| Local goal-lane fix after reviewing `20260628_231246_completion_alignment_check` video | The user observed that after the left turn the EV first enters the right-side exit lane, then drifts/lane-changes into the left-side exit lane before the rollout ends. Trajectory/debug data confirmed this is a route geometry issue rather than a risk-allocation issue: around steps `232-240`, the EV is already straight in the first exit lane (`epsi≈-0.02..0.055`), but relative to the previous goal route it has `ey≈-3.1m`; later the reference pulls it laterally until `ey≈0`, producing the visible lane change. | Added tuned ego `goal_left_offset: 1.85 -> 0.0` in `give_way_smpc_tuning.json`. This selects the natural/first exit lane after the left turn instead of forcing the post-turn path toward the adjacent lane. Kept `goal_longitudinal_offset=30.0` and the strict completion thresholds so the vehicle can align within that same lane before completion. | Local validation only: tuning JSON is valid, Python compile still passes, and tuning injection now resolves `start_left_offset=2.75`, `goal_left_offset=0.0`, `goal_longitudinal_offset=30.0`, `completion_s_margin=2.0`, `completion_goal_dist=4.0`, `completion_lateral_error=1.5`, `completion_heading_error=0.1`. | Re-run the completion-alignment check after syncing code. Expected visual effect: EV turns left into one exit lane and continues in that lane; it should no longer perform a right-lane-to-left-lane drift before ending. |
| Re-run of `20260628_231246_completion_alignment_check` after `goal_left_offset=0.0` | Reused the same results directory after syncing the horizon-padding and goal-lane-offset fixes. | Full batch completed all 5 subruns. The server post-CARLA gate file was initially stale from the earlier failed run, so the gate was regenerated on the server and pulled locally. Applied config confirms `goal_left_offset=0.0`, `goal_longitudinal_offset=30.0`, strict completion thresholds, and `risk_profile=adaptive_interaction_severity`. | Overall gate is `FAIL`, but not because of collision, yield order, or solver robustness. `smpc_var_risk`: center dmin `5.320m`, no footprint collision, yield order OK, solver failure `0.000`, but `completion_valid=None`. `smpc_fixed_risk`: center dmin `4.806m`, no footprint collision, yield order OK, solver failure `0.000`, but `completion_valid=None`. `smpc_open_loop` remains non-required WARN: no collision but yield-order failure. Debug shows both required SMPC policies run to `600` steps without producing `smpc_completion.json`. For var-risk, the closest goal distance occurs around step `391` (`goal_dist=2.733m`) but `ey=3.269m`, `epsi=0.333rad`, so both lateral and heading completion fail. By the final step, `s=60.0`, `s_to_end=0`, but `goal_dist=16.536m`, `ey=3.147m`, `epsi=-0.324rad`, indicating the vehicle has passed the route end while the reference is clamped/frozen. | The `goal_left_offset=0.0` change is not an acceptable final fix. It removes the earlier explicit lane-change target but creates an unreachable/mismatched completion geometry under the current reference route. This result should not be used as dissertation evidence. Next fix should not only relax completion; it should redesign the post-turn goal/route so the EV enters exactly one selected exit lane and the reference endpoint, visual lane, and completion goal agree. |
| Local redesigned post-turn completion configuration | Designed a more conservative configuration after the failed `goal_left_offset=0.0` trial. Goal: keep the EV in one selected exit lane and avoid both old early diagonal completion and the new 30m downstream lane-correction/route-end clamp. | Updated `give_way_smpc_tuning.json`: `goal_left_offset: 0.0 -> 1.85` (restore the previously validated scenario lane), `goal_longitudinal_offset: 30.0 -> 22.0` (only a short extension beyond the original 20m, not the rejected 30m), `completion_s_margin: 2.0 -> 3.0`, `completion_goal_dist: 4.0 -> 5.0`, `completion_lateral_error: 1.5 -> 2.0`, `completion_heading_error: 0.10 -> 0.18rad`. | Local validation only: JSON valid, affected Python files compile, and tuning injection resolves `start_left_offset=2.75`, `goal_left_offset=1.85`, `goal_longitudinal_offset=22.0`, `completion_s_margin=3.0`, `completion_goal_dist=5.0`, `completion_lateral_error=2.0`, `completion_heading_error=0.18`. | Expected effect: route remains close to the previously successful lane geometry, but the rollout has a small extra alignment segment and a heading-aware completion check. This should reduce visible diagonal ending without forcing a second downstream lane change or route-end clamp. Must be validated with the same `completion_alignment_check` CARLA command before using for final dissertation runs. |
| Local route-end low-speed stagnation fix | The user pointed out that in the failed completion-alignment video the EV slowed to near zero and stayed too long after entering the exit area. This is the same failure mode as `completion_valid=None`: after the routed path reaches its final waypoint, the reference horizon becomes a repeated endpoint, so SMPC can keep trying to reduce residual lateral/heading error at very low speed instead of continuing naturally. | Added `post_goal_reference_extension_m=12.0` to the ego tuning and `SMPCAgent`. During initial route generation, the high-level CARLA route is extended by a 12m straight segment along the final waypoint heading before constructing the Frenet trajectory. This keeps the reference moving in the same lane after the nominal goal, preventing endpoint freezing. | Local validation only: JSON valid, Python compile passed, and tuning injection resolves `post_goal_reference_extension_m=12.0` together with the redesigned `goal_left_offset=1.85`, `goal_longitudinal_offset=22.0`, and relaxed strict completion thresholds. | Expected effect: if the ego reaches the goal area before completion fires, it should continue forward in the same exit lane rather than slowing/stagnating around the final waypoint. This should be validated in CARLA; it is a direct fix for route-end stagnation, not a safety/yield modification. |
| `20260704_125801_completion_alignment_check` | Validation run for the redesigned post-turn completion configuration and `post_goal_reference_extension_m=12.0`. | Full 5-subrun CARLA batch completed and generated videos/metrics. Applied config: `goal_left_offset=1.85`, `goal_longitudinal_offset=22.0`, `completion_s_margin=3.0`, `completion_goal_dist=5.0`, `completion_lateral_error=2.0`, `completion_heading_error=0.18`, `post_goal_reference_extension_m=12.0`. | Overall gate `FAIL` because required SMPC policies did not complete validly. Safety/rule checks are still good: `smpc_var_risk` center dmin `4.062m`, no footprint collision, yield OK, solver failure `0.000`; `smpc_fixed_risk` center dmin `4.149m`, no footprint collision, yield OK, solver failure `0.000`. Both required policies run to 600 steps and lack `smpc_completion.json`. Debug shows the new post-goal extension took effect (`end_s=62.0`), but completion misses the goal-area window: var-risk closest goal distance is `0.210m` at step `271`, with `ey=-1.445m` and `epsi=0.266rad`; lateral is OK but heading fails the strict `0.18rad` threshold. Later, heading becomes OK but lateral error grows to about `3.03m`, and speed drops to `0.11m/s` at `s=62.0`, recreating route-end stagnation at the extended endpoint. | Not usable as final result. The direct extension helped avoid the old endpoint but did not make completion robust. Next change should either relax heading completion near the goal-area window (e.g. to about `0.30rad`) or define a lane-entry completion condition based on being close to the goal with lateral OK before the downstream extension pulls the car away. Do not include this run in dissertation quantitative results. |
| Local lane-entry completion fix after `20260704_125801` | Directly targets the missed completion window found in `20260704_125801`: the ego was effectively at the routed goal (`goal_dist=0.210m`) and laterally within the selected lane (`ey=-1.445m`) but failed the strict heading gate (`epsi=0.266rad > 0.18rad`). It then continued onto the post-goal extension, lateral error grew to about `3.03m`, and the vehicle stagnated near the extended endpoint. | Added two ego tuning parameters and matching `SMPCAgent` logic: `completion_lane_entry_goal_dist=1.0m` and `completion_lane_entry_heading_error=0.30rad`. Normal route-end/broad goal completion still uses the stricter `completion_heading_error=0.18rad`; only the near-goal lane-entry window can use the wider `0.30rad` heading tolerance. Added `completed_by_lane_entry` to `smpc_completion.json`, `compute_scenario_results.py`, and `postcarla_trajectory_gate.py` so metrics/gate treat this as valid completion. | Local validation: JSON valid, affected Python files compile, tuning injection resolves `completion_lane_entry_goal_dist=1.0` and `completion_lane_entry_heading_error=0.30`. Replaying the failed debug state (`goal_dist=0.210`, `ey=-1.445`, `epsi=0.266`) gives `lateral_ok=True`, `strict_heading_ok=False`, `lane_entry_goal_ok=True`, `lane_entry_heading_ok=True`, hence `completed_by_lane_entry=True`. Local runtime import of postprocess modules was not possible with system Python because it lacks `numpy`; server `carla_modern` should be used for full validation. | Expected effect: the CARLA rollout should end around the physically reasonable lane-entry moment instead of continuing to the extension endpoint and slowing to near zero. Re-run the same `completion_alignment_check` before updating final dissertation results. |
| Re-run of `20260704_125801_completion_alignment_check` after lane-entry completion | The same result directory was overwritten by a second CARLA batch after syncing the lane-entry completion fix. The original gate file was stale from the earlier failed run and had to be regenerated on the server. | Applied config confirms `goal_left_offset=1.85`, `goal_longitudinal_offset=22.0`, `completion_lateral_error=2.0`, `completion_heading_error=0.18`, `completion_lane_entry_goal_dist=1.0`, `completion_lane_entry_heading_error=0.30`, and `post_goal_reference_extension_m=12.0`. Regenerated post-CARLA gate reports `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. | Required policies now pass. `smpc_fixed_risk`: PASS, center dmin `4.150m`, no footprint collision, yield OK, solver failure `0.000`, completion step `271`, completion time `13.5s`, `goal_dist=0.757m`, `ey=-1.629m`, `epsi=0.250rad`, speed `5.171m/s`, `completed_by_lane_entry=1`. `smpc_var_risk`: PASS, center dmin `3.862m`, no footprint collision, yield OK, solver failure `0.000`, completion step `292`, completion time `14.55s`, `goal_dist=0.839m`, `ey=-1.576m`, `epsi=0.245rad`, speed `5.131m/s`, `completed_by_lane_entry=1`. `smpc_open_loop`: WARN, no collision but yield order false. | Data-level fix is successful: required SMPC no longer runs to 600 steps or slows to near-zero at the extended route end. Because local `ffmpeg` is unavailable, final visual-frame inspection was not performed locally; user should still confirm the video visually. If video is acceptable, freeze this completion logic and run the final dissertation batch under a fresh results directory. |
| Local exit-lane alignment trajectory fix | The user observed that the lane-entry completion run still lets the EV enter the target lane diagonally: the data confirms completion happened with `epsi≈0.245-0.250rad` and speed about `5.1m/s`. This solved stagnation but accepted a visibly angled lane entry. | Modified the SMPC reference generation rather than only the endpoint/completion gate. Added `exit_alignment_speed=3.0m/s`, `exit_alignment_distance_before_goal=12.0m`, and `exit_alignment_distance_after_goal=12.0m`. During `fit_velocity_profile()`, the reference speed is capped to `3.0m/s` from 12m before the routed goal through the 12m post-goal extension, giving the controller time to align with the exit-lane heading. Tightened `completion_lane_entry_heading_error: 0.30 -> 0.18rad`, so lane-entry completion can no longer accept the previous diagonal `epsi≈0.25rad` state. Defaults in `VehicleParams` and `SMPCAgent` were also changed to `0.18rad` to avoid regressions without tuning config. | Local validation only: JSON valid, affected Python files compile, and tuning injection resolves `completion_lane_entry_heading_error=0.18`, `exit_alignment_speed=3.0`, `exit_alignment_distance_before_goal=12.0`, and `exit_alignment_distance_after_goal=12.0`. | Expected effect: the EV should slow in the exit-lane alignment segment and continue until its heading is closer to the target lane direction before completion, reducing the visible diagonal lane entry. Re-run `completion_alignment_check`; if it still enters diagonally, the next step is not another completion threshold change but explicit spatial route shaping with an inserted same-lane alignment waypoint. |
| Re-run of `20260704_125801_completion_alignment_check` with `exit_alignment_speed=3.0` | Server result was written into the same directory again. Top-level files updated at about 14:12, while stale `smpc_completion.json` files from the previous 13:37/13:41 lane-entry-completion run remained in the subrun directories. The stale completion files polluted `paper_metrics_summary.md`, so the post-CARLA gate was regenerated manually on the server for the authoritative result. | Applied config confirms `completion_lane_entry_heading_error=0.18`, `exit_alignment_speed=3.0`, `exit_alignment_distance_before_goal=12.0`, `exit_alignment_distance_after_goal=12.0`, and `post_goal_reference_extension_m=12.0`. | Authoritative regenerated gate: `Overall: FAIL`. `smpc_var_risk`: PASS, center dmin `5.270m`, no footprint collision, yield OK. `smpc_fixed_risk`: FAIL, center dmin `2.050m`, footprint collision `18.65s`, yield order false (`ego_enter=2521.88s`, `target_exit=2523.78s`), and `solver_failure_frac=0.292 > 0.050`. `smpc_open_loop`: non-required WARN, no collision but yield order false. Batch summary confirms the new run actually used 600 steps for both required SMPC policies, and fixed-risk feasibility fell to `0.285`. | This configuration must not be used. The `3.0m/s` exit-alignment speed cap is too aggressive and destabilizes the fixed-risk SMPC / yield interaction. Revert or relax this approach. The safer next direction is explicit spatial route shaping with an inserted exit-lane alignment waypoint while preserving the previously safe lane-entry completion configuration, not further lowering speed or tightening completion globally. Also avoid reusing result directories because stale completion files can corrupt postprocess tables. |
| Local rollback plus spatial exit-route shaping | The user requested rollback to the best version before the failed `exit_alignment_speed=3.0` trial, then optimisation without disturbing the validated pre-yield/yield behaviour. | Removed the failed exit-alignment speed cap and its tuning fields. Restored the last safe lane-entry completion tolerance `completion_lane_entry_heading_error=0.30`. Added spatial route shaping instead: `exit_alignment_path_enabled=true`, `exit_alignment_path_length=10.0m`. In `SMPCAgent.reference_regeneration()`, after CARLA `trace_route()` and before the post-goal extension, the final 10m of the route is replaced by a same-lane straight alignment segment along the routed goal waypoint yaw. This changes only the post-turn exit geometry and leaves the yield supervisor, risk allocation, stop buffer, release clearance, and recovery logic unchanged. | Local validation: JSON valid, affected Python files compile, tuning injection confirms `completion_lane_entry_heading_error=0.30`, `post_goal_reference_extension_m=12.0`, `exit_alignment_path_enabled=True`, `exit_alignment_path_length=10.0`, `yield_stop_buffer_distance=8.0`, and `yield_release_clearance_margin=1.0`. | Expected effect: keep the safety/completion behaviour of the previous best lane-entry version while making the reference path enter the target lane along the lane direction, reducing visible diagonal entry without the fixed-risk infeasibility introduced by speed capping. Re-run a fresh `completion_alignment_check` in a new result directory. |
| `20260704_142224_exit_path_alignment_check` | Fresh server run after rollback from the failed speed-cap experiment and enabling `exit_alignment_path_enabled=true`, `exit_alignment_path_length=10.0m`. | Applied config: `completion_lane_entry_heading_error=0.30`, `post_goal_reference_extension_m=12.0`, `exit_alignment_path_enabled=true`, `exit_alignment_path_length=10.0`, `yield_stop_buffer_distance=8.0`, `yield_release_clearance_margin=1.0`. | Gate `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. Required SMPC both PASS. `smpc_var_risk`: center dmin `3.949m`, no footprint collision, yield OK, solver failure `0.000`, completion step `292`, `goal_dist=0.861m`, `ey=-1.569m`, `epsi=0.247rad`, speed `5.006m/s`. `smpc_fixed_risk`: center dmin `4.035m`, no footprint collision, yield OK, solver failure `0.000`, completion step `270`, `goal_dist=0.970m`, `ey=-1.699m`, `epsi=0.244rad`, speed `5.102m/s`. `smpc_open_loop`: non-required WARN, no collision but yield order false. | Safety and feasibility recovered after reverting the `3.0m/s` speed cap, but diagonal lane entry is not materially improved: completion heading error remains about `0.244-0.247rad`, close to the previous lane-entry run. The final 10m route shaping is too weak or applied too late relative to the vehicle state at completion (`s≈50`, `s_to_end=12`). Next visual-quality fix should inspect/shape the reference before `s≈50`, likely by inserting an earlier explicit exit-lane alignment waypoint or shifting the routed goal/alignment start upstream; do not change yield-before-conflict logic. |
| Local pending after `20260704_142224_exit_path_alignment_check` | Reworked the visual-heading fix based on prior failures/successes and external path-tracking guidance: vehicle heading must be corrected by the reference path and completion heading gate together, not by endpoint distance alone. | Kept validated pre-yield/yield settings unchanged (`yield_stop_buffer_distance=8.0`, `yield_release_clearance_margin=1.0`, adaptive risk profile unchanged). Kept strict completion with `completion_lane_entry_heading_error=0.18`, `completion_exit_alignment_min_s_after_goal=4.0`, and `post_goal_reference_extension_m=12.0`. Replaced the previous two-point final straight shaping with a Hermite transition plus same-lane straight tail, increased `exit_alignment_path_length=10.0 -> 18.0`, and widened the post-clearance-only speed-cap window `exit_alignment_post_clearance_goal_window=8.0 -> 14.0` while keeping `exit_alignment_post_clearance_speed=4.0`. | Local validation: tuning JSON valid; affected Python files compile; injected tuning confirms `completion_lane_entry_heading_error=0.18`, `completion_exit_alignment_min_s_after_goal=4.0`, `post_goal_reference_extension_m=12.0`, `exit_alignment_path_enabled=True`, `exit_alignment_path_length=18.0`, `exit_alignment_post_clearance_speed=4.0`, `exit_alignment_post_clearance_goal_window=14.0`, `yield_stop_buffer_distance=8.0`, and `yield_release_clearance_margin=1.0`. | Re-run a fresh `exit_alignment_completion_check`. Success requires both required SMPC policies to stay PASS with `solver_failure_frac=0.000`, no footprint collision, valid yield order, and completion with `abs(epsi)<=0.18`; visually, the EV should start aligning before the出口车道入口 rather than completing while diagonal. If this still fails visually, inspect the generated reference and move the alignment start further upstream instead of changing yield logic. |
| `20260704_145152_exit_alignment_completion_check` | Server validation of the Hermite 18m exit-alignment shaping and strict exit-alignment completion. | Applied config confirms `completion_lane_entry_heading_error=0.18`, `completion_exit_alignment_min_s_after_goal=4.0`, `post_goal_reference_extension_m=12.0`, `exit_alignment_path_enabled=true`, `exit_alignment_path_length=18.0`, `exit_alignment_post_clearance_speed=4.0`, `exit_alignment_post_clearance_goal_window=14.0`, with validated yield settings unchanged. | Gate `Overall: FAIL` only because both required SMPC policies did not complete validly. Safety/yield/solver are still good: `smpc_var_risk` center dmin `4.151m`, no footprint collision, yield OK, solver failure `0.000`; `smpc_fixed_risk` center dmin `4.160m`, no footprint collision, yield OK, solver failure `0.000`. Completion failed for both after the full 600 steps. At closest approach to the original goal, goal distance was about `2.1-2.3m` but `epsi≈0.41rad`. Later, heading became briefly acceptable only when lateral error was too large (`ey≈3.0m`), and no frame satisfied both `abs(epsi)<=0.18` and `abs(ey)<=2.0` after the routed goal. Final state regressed into tail-end crawling/stall: var-risk `speed≈0.08m/s`, `epsi≈-0.30rad`, `goal_dist≈15.6m`; fixed-risk `speed≈0.94m/s`, `epsi≈-0.41rad`, `goal_dist≈16.1m`. | Reject this configuration for visual completion. The 18m Hermite shaping is too aggressive/late and creates an unreachable tail-alignment task; it preserves rule safety but worsens completion and reintroduces low-speed tail stagnation. Next fix should roll back from 18m shaping and instead use a lane-consistent endpoint/exit lane selection or a bounded final alignment objective that does not let the reference tail pull the vehicle far past the original goal. Do not alter the validated pre-yield/yield supervisor. |
| Local rollback after `20260704_145152_exit_alignment_completion_check` | Restored the safe completion baseline and switched to a gentler experiment method. | Reverted the rejected visual-forcing pieces: `completion_lane_entry_heading_error=0.18 -> 0.30`, `exit_alignment_path_length=18.0 -> 10.0`, `exit_alignment_post_clearance_goal_window=14.0 -> 0.0`, and replaced the 18m Hermite exit path with the earlier gentle two-point same-lane straight tail. Kept `completion_heading_error=0.18` as the strict visual-quality diagnostic and kept `completion_exit_alignment_min_s_after_goal=4.0` only for metrics compatibility. Yield/risk settings remain unchanged: `yield_stop_buffer_distance=8.0`, `yield_release_clearance_margin=1.0`, adaptive interaction-severity risk profile unchanged. | Local validation: tuning JSON valid; affected Python files compile; injected tuning confirms `completion_heading_error=0.18`, `completion_lane_entry_heading_error=0.30`, `post_goal_reference_extension_m=12.0`, `exit_alignment_path_enabled=True`, `exit_alignment_path_length=10.0`, `exit_alignment_post_clearance_goal_window=0.0`, `yield_stop_buffer_distance=8.0`, and `yield_release_clearance_margin=1.0`. | Re-run as a fresh `gentle_exit_baseline_check`. Expected effect is to recover the `20260704_142224` safety/completion behavior and eliminate the 600-step tail stagnation. Judge visual progress by the logged `completion_heading_ok`/`epsi`, not by making strict heading a hard completion gate again. If heading remains around `0.24-0.25rad`, the next method should be lane-consistent endpoint selection upstream of the goal rather than stronger tail shaping. |
| `20260704_151615_gentle_exit_baseline_check` | Server validation of the gentler rollback after rejecting the 18m Hermite/strict-heading completion trial. | Applied config confirms `completion_heading_error=0.18`, `completion_lane_entry_heading_error=0.30`, `post_goal_reference_extension_m=12.0`, `exit_alignment_path_enabled=true`, `exit_alignment_path_length=10.0`, `exit_alignment_post_clearance_goal_window=0.0`, and the validated yield settings unchanged. | Gate `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. Required SMPC both PASS. `smpc_var_risk`: completion step `312`, completion time `15.55s`, center dmin `4.160m`, no footprint collision, yield OK, solver failure `0.000`, `goal_dist=0.860m`, `ey=-1.579m`, `epsi=0.253rad`, speed `5.003m/s`, `completed_by_lane_entry=1`, `completion_heading_ok=0`. `smpc_fixed_risk`: completion step `311`, completion time `15.50s`, center dmin `4.171m`, no footprint collision, yield OK, solver failure `0.000`, `goal_dist=0.812m`, `ey=-1.551m`, `epsi=0.260rad`, speed `5.013m/s`, `completed_by_lane_entry=1`, `completion_heading_ok=0`. The 600-step tail stagnation from `20260704_145152` is gone. | Rollback succeeded as a safety/completion baseline but did not solve the visual heading problem; heading remains about `0.25-0.26rad`, similar to the earlier lane-entry runs. Do not return to strict heading completion or stronger tail shaping. The next visual experiment should change the lane-consistent endpoint/reference geometry before the lane-entry point, or add a bounded heading-quality metric/objective that does not control termination. |
| Local route-only downstream goal extension after `20260704_151615_gentle_exit_baseline_check` | Implements the next visual-heading experiment without moving the task completion goal and without forcing post-goal termination. | Added `route_goal_extension_m=6.0`. In `SMPCAgent.reference_regeneration()`, the CARLA `trace_route()` target is extended 6m along the exit-lane goal waypoint yaw, but `self.goal_location` remains the original scenario/tuning goal for `goal_dist` and completion metrics. `_route_goal_s` is now recorded as the nearest route arclength to the original goal, not the extended route endpoint. Kept safety baseline completion and yield settings: `completion_lane_entry_heading_error=0.30`, `completion_heading_error=0.18` diagnostic, `exit_alignment_path_length=10.0`, `exit_alignment_post_clearance_goal_window=0.0`, `yield_stop_buffer_distance=8.0`, `yield_release_clearance_margin=1.0`. | Local validation: tuning JSON valid; affected Python files compile; injection confirms `route_goal_extension_m=6.0` and all safety/yield baseline values unchanged. | Re-run as fresh `route_goal_extension_check`. Expected effect: the route reference should start aligning with the exit lane before reaching the original completion goal, potentially reducing `epsi` at lane-entry completion without causing the 600-step tail chase. Success means required SMPC PASS, no collision, yield OK, solver failure `0.000`, completion still by lane-entry near original goal, and `epsi` materially lower than `0.25-0.26rad`. |
| `20260704_153721_route_goal_extension_check` | Server validation of the route-only downstream goal extension experiment. | Applied config confirms `route_goal_extension_m=6.0`, `completion_lane_entry_heading_error=0.30`, `completion_heading_error=0.18`, `post_goal_reference_extension_m=12.0`, `exit_alignment_path_length=10.0`, `exit_alignment_post_clearance_goal_window=0.0`, with yield settings unchanged. | Gate `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. Required SMPC both PASS. `smpc_var_risk`: completion step `271`, completion time `13.50s`, center dmin `4.036m`, no footprint collision, yield OK, solver failure `0.000`, `goal_dist=0.848m`, `ey=-1.650m`, `epsi=0.252rad`, speed `5.030m/s`, `completed_by_lane_entry=1`, `completion_heading_ok=0`. `smpc_fixed_risk`: completion step `271`, completion time `13.50s`, center dmin `3.936m`, no footprint collision, yield OK, solver failure `0.000`, `goal_dist=0.852m`, `ey=-1.648m`, `epsi=0.257rad`, speed `5.110m/s`, `completed_by_lane_entry=1`, `completion_heading_ok=0`. No 600-step tail stagnation. | Route-only downstream extension preserves safety/completion but does not materially improve the visual heading problem. Compared with `20260704_151615`, `epsi` improves only from `0.253 -> 0.252rad` for var-risk and `0.260 -> 0.257rad` for fixed-risk. This is within practical noise and not enough for visual lane-entry alignment. Next direction should not be larger downstream extension; it should inspect the actual lane-entry geometry/video and modify the turn path before the original goal, likely through an explicit intermediate lane-entry/heading waypoint or a bounded heading objective near the lane-entry zone that does not control termination. |
| Local goal-anchored lane-entry alignment after `20260704_153721_route_goal_extension_check` | Implements the next visual-heading experiment based on the route-extension failure: the heading cue must start before the original completion goal, not at the downstream route endpoint. | Disabled route-only extension with `route_goal_extension_m=0.0`. Reworked `_apply_exit_alignment_path_shaping()` so the same-lane alignment segment is anchored around the original task/completion goal: it starts `exit_alignment_path_length=12.0m` before the original goal and continues `exit_alignment_distance_after_goal=6.0m` after it. Re-enabled a mild post-clearance-only speed cap near the original goal: `exit_alignment_post_clearance_speed=4.5m/s`, `exit_alignment_post_clearance_goal_window=10.0m`. Kept safe completion and yield settings unchanged: `completion_lane_entry_heading_error=0.30`, `completion_heading_error=0.18` diagnostic, `yield_stop_buffer_distance=8.0`, `yield_release_clearance_margin=1.0`. | Local validation: tuning JSON valid; affected Python files compile; injection confirms `route_goal_extension_m=0.0`, `exit_alignment_path_length=12.0`, `exit_alignment_distance_after_goal=6.0`, `exit_alignment_post_clearance_speed=4.5`, `exit_alignment_post_clearance_goal_window=10.0`, and yield settings unchanged. | Re-run as fresh `goal_anchored_lane_entry_alignment_check`. Success means required SMPC still PASS, no collision, yield OK, solver failure `0.000`, no 600-step tail stagnation, and completion epsi materially lower than the `0.25-0.26rad` baseline. If this improves heading but slows completion mildly, it is acceptable; if it hurts safety/yield, revert the speed cap first before changing the geometry. |
| `20260704_232914_goal_anchored_lane_entry_alignment_check` | Server validation of the goal-anchored lane-entry alignment experiment. Server batch did not initially include a gate file, so the post-CARLA gate was regenerated on the server and pulled back. | Applied config confirms `route_goal_extension_m=0.0`, `exit_alignment_path_length=12.0`, `exit_alignment_distance_after_goal=6.0`, `exit_alignment_post_clearance_speed=4.5`, `exit_alignment_post_clearance_goal_window=10.0`, with yield settings unchanged. | Regenerated gate `Overall: FAIL`. Required SMPC both fail only because completion is invalid; no footprint collision and yield order are still OK. `smpc_var_risk`: center dmin `3.976m`, no footprint collision, yield OK, solver failure `0.000`, ran full `600` steps. Closest-to-goal frame: step `285`, `goal_dist=0.569m`, `ey=0.490m`, but `epsi=0.363rad`, so no lane-entry completion. It then overshot to route end and slowed (`speed≈0.427m/s` at step `599`, `goal_dist≈32.7m`). `smpc_fixed_risk`: center dmin `7.212m`, no footprint collision, yield OK, solver failure `0.000`, ran full `600` steps, but diverged in recovery and never reached the goal region; final `goal_dist≈29.4m`, `ey≈9.26m`, `epsi≈0.65rad`, `speed≈0.51m/s`. | Reject this configuration. Anchoring a 12m same-lane segment around the original goal is too strong and creates a path-tracking problem before completion; the post-clearance speed cap also likely contributes to route-end/tail crawling. This is worse than the `20260704_151615` safety baseline. Next action should roll back to that baseline and, if continuing visual refinement, use a much weaker diagnostic-only intermediate waypoint/metric or inspect the CARLA video/map geometry directly before making more path-shaping changes. |
| Local anti-early-stop safe-baseline experiment after `20260704_232914_goal_anchored_lane_entry_alignment_check` | Rolls back the failed goal-anchored shaping while testing a smaller hypothesis for the visual heading issue: the safe baseline may terminate slightly before the original route goal, so the final video frame catches the ego while yaw is still settling. | Restored safe-baseline geometry and speed settings: `route_goal_extension_m=0.0`, route-end `exit_alignment_path_length=10.0`, `exit_alignment_distance_after_goal=0.0`, disabled post-clearance cap with `exit_alignment_post_clearance_goal_window=0.0`, retained `exit_alignment_post_clearance_speed=4.0` as inactive. Added `completion_lane_entry_min_s_after_route_goal=0.0` and required this for lane-entry completion, so completion cannot fire while `s_after_route_goal<0`. This is deliberately much weaker than the rejected strict `completion_exit_alignment_min_s_after_goal=4.0` path: it delays the safe-baseline completion by about the observed `0.4m` early-stop margin rather than forcing several meters of post-goal heading alignment. Yield settings remain unchanged. | Local validation: tuning JSON valid; affected Python files compile; injection confirms `completion_lane_entry_min_s_after_route_goal=0.0`, `exit_alignment_path_length=10.0`, speed cap window disabled, and validated yield settings unchanged. | Re-run as fresh `anti_early_stop_lane_entry_check`. Success means required SMPC PASS, no collision, yield OK, solver failure `0.000`, no 600-step stagnation, and completion occurs at `s_after_route_goal>=0.0`. This may or may not fully solve visual yaw, but it isolates the lowest-risk explanation before attempting another control/path modification. If epsi remains `0.25-0.26rad`, the next method should be a bounded heading cost/metric in the MPC objective or direct map/video geometry inspection, not stronger path shaping. |
| `20260705_000040_anti_early_stop_lane_entry_check` | Server validation of the anti-early-stop safe-baseline experiment. The initially pulled gate file was stale and reported `FAIL`; regenerating `postcarla_trajectory_gate.py` on the server produced the correct current report. | Applied setup confirms the intended anti-early-stop configuration: `completion_lane_entry_min_s_after_route_goal=0.0`, `exit_alignment_path_length=10.0`, `exit_alignment_distance_after_goal=0.0`, `exit_alignment_post_clearance_goal_window=0.0`, `route_goal_extension_m=0.0`, with yield settings unchanged. | Regenerated gate `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. Required SMPC both PASS. `smpc_var_risk`: completion step `271`, `goal_dist=0.548m`, `ey=-1.595m`, `epsi=0.247rad`, `s_after_route_goal=0.590m`, center dmin `4.139m`, no footprint collision, yield OK, solver failure `0.000`. `smpc_fixed_risk`: completion step `312`, `goal_dist=0.496m`, `ey=-1.472m`, `epsi=0.259rad`, `s_after_route_goal=0.590m`, center dmin `4.159m`, no footprint collision, yield OK, solver failure `0.000`. | The anti-early-stop guard works as a safety/completion fix: it prevents completion before the original route goal and still avoids 600-step stagnation. It does not solve the visual heading problem; `epsi` remains around the same `0.25-0.26rad` baseline. Do not continue with stronger path shaping. Treat this as a diagnostic-safe local visual branch, while full-experiment expansion should use the validated `20260628_103325_final_dissertation` full-method lineage. |
| Local Stage A lane-entry heading diagnostics implementation | Implements the geometry-only diagnostic plan for the post-turn heading issue without changing controller behaviour. | Added `smpc_lane_entry_heading_diagnostics.json` and `.csv` export from `SMPCAgent` when `goal_dist<=8m`, `s_after_route_goal>=-8m`, or completion fires. Each row records ego pose, Frenet reference pose/yaw, CARLA map waypoint pose/yaw/lane id/road id, goal yaw, and residuals `ego_minus_ref_yaw`, `ego_minus_map_yaw`, and `ref_minus_map_yaw`. Added executable script `core/scripts/carla/run_lane_entry_heading_diagnostics_check.sh`, which runs the current give-way scenario with `smpc_var_risk`, `smpc_fixed_risk`, `smpc_open_loop`, `notv`, and `notv_cl`, then runs the post-CARLA gate and calls `core/scripts/summarize_lane_entry_heading_diagnostics.py` for a compact diagnostics summary. | Local validation: `smpc_agent.py`, `run_intersection_scenario.py`, and the summary script compile; shell script passes `bash -n`; scripts are executable. | Re-run on server as fresh `lane_entry_heading_diagnostics_check`. The expected gate should match the anti-early-stop safe branch: required SMPC PASS, open-loop WARN acceptable. Use the diagnostics residuals to decide whether the next intervention should be a bounded heading objective (`ego_minus_ref_yaw` dominant) or a local yaw blend (`ref_minus_map_yaw` dominant). |
| `20260705_004623_lane_entry_heading_diagnostics_check` | Server Stage A geometry-only diagnosis for the post-turn heading issue. | Pulled fresh result directory via direct `scp -r`. Gate and metrics confirm a complete run. The diagnostic CSV/JSON files were produced for both required SMPC policies. A minor local summary-script issue was fixed afterward: it now reports the first completion row, and future diagnostics only tag the first completion event. | Gate `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. Required SMPC both PASS with zero solver failures and no footprint collision. `smpc_var_risk`: completion step `271`, `goal_dist=0.573m`, `ey=-1.611m`, `epsi=0.253rad`, `s_after_route_goal=0.590m`, center dmin `4.144m`. `smpc_fixed_risk`: completion step `269`, `goal_dist=0.493m`, `ey=-1.595m`, `epsi=0.256rad`, `s_after_route_goal=0.590m`, center dmin `4.151m`. Stage A residuals at first completion: for var-risk `ego_minus_ref_yaw=0.253`, `ego_minus_map_yaw=0.253`, `ref_minus_map_yaw=0.000`; for fixed-risk `ego_minus_ref_yaw=0.256`, `ego_minus_map_yaw=0.256`, `ref_minus_map_yaw=0.000`. | Diagnosis: the reference yaw matches the CARLA map lane yaw exactly in the lane-entry zone, so the vehicle-heading issue is not a route/reference yaw geometry error. The dominant residual is ego yaw versus reference/map yaw. Next experiment should be a bounded lane-entry heading objective after target clearance and near the original goal. Do not use local yaw blend or stronger path shaping for this issue. |
| Local Stage B1 bounded lane-entry heading objective implementation | Implements the Stage B1 intervention selected by Stage A diagnostics. | Added a horizon-wise non-negative `heading_cost_weights` parameter to `SMPC_MMPreds` and an additional quadratic heading-error cost term. Default weights are zero, so behaviour is unchanged unless `SMPCAgent` activates them. Added `lane_entry_heading_cost_enabled`, `lane_entry_heading_cost_goal_window`, `lane_entry_heading_cost_weight`, and `lane_entry_heading_cost_max_abs_epsi` to `VehicleParams`, `SMPCAgent`, tuning config, debug setup, and per-step debug logs. The cost activates only when `target_cleared_conflict=True`, the horizon reference points are within the original-goal window, the policy is var/fixed SMPC, and current `|epsi|` is within the configured bound. Added `run_lane_entry_heading_cost_check.sh` for fresh server validation. | Local validation: tuning JSON valid; `mpc_utils.py`, `smpc_agent.py`, `run_intersection_scenario.py`, and diagnostics summary script compile; shell scripts pass syntax checks. Active tuning values: enabled `true`, goal window `8.0m`, weight `0.2`, max `|epsi|=0.35rad`. | Run as fresh `lane_entry_heading_cost_check`. Expected minimum acceptance: required SMPC PASS, no footprint collision, yield OK, solver failure `0.000`, no 600-step non-completion. Desired visual improvement: completion `epsi` drops below the Stage A `0.253/0.256rad` baseline without changing yield behaviour. If safety regresses or solver failures appear, reduce `lane_entry_heading_cost_weight` before trying any route/path shaping. |
| `20260705_114259_lane_entry_heading_cost_check` | Server Stage B1 validation for bounded lane-entry heading objective. | Pulled fresh result directory via direct `scp -r`. Stage B1 settings were active in `smpc_debug_setup.json`: enabled `true`, goal window `8.0m`, weight `0.2`, max `|epsi|=0.35rad`. Debug traces confirm activation only after target clearance and near the original goal: var-risk had 80 active steps and fixed-risk had 79 active steps. | Gate `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. Required SMPC both PASS with no footprint collision, yield OK, and `solver_failure_frac=0.000`. `smpc_var_risk`: center dmin `4.151m`, completion step `272`, `goal_dist=0.516m`, `ey=-1.535m`, `epsi=0.267rad`, `s_after_route_goal=0.590m`. `smpc_fixed_risk`: center dmin `4.142m`, completion step `272`, `goal_dist=0.532m`, `ey=-1.588m`, `epsi=0.251rad`, `s_after_route_goal=0.590m`. Diagnostics still show `ref_minus_map_yaw=0.0` at completion. | Outcome: accept as safe but reject as a heading fix. Compared with Stage A (`0.253/0.256rad`), var-risk worsened and fixed-risk improved only marginally. The weight `0.2` is too weak/late to reliably reduce completion heading error before lane-entry completion triggers. Do not use this as full-experiment mainline. Next heading-specific trial, if pursued, should either slightly increase the bounded heading-cost authority and delay only lane-entry completion by a small positive `s_after_route_goal`, or test a terminal heading cost on the first completion-eligible horizon point. The full dissertation mainline remains `20260628_103325_final_dissertation`. |
| Local Stage B1.1 stronger bounded heading objective plus small completion delay | Implements the next trial recommended by Stage B1 analysis, without returning to route/path shaping and without changing the validated yield supervisor. | Increased `lane_entry_heading_cost_weight` from `0.2` to `0.4` so the already-active bounded heading objective has more authority after `target_cleared_conflict=True` near the original goal. Increased `completion_lane_entry_min_s_after_route_goal` from `0.0m` to `1.0m`, preventing lane-entry completion at the recurring `s_after_route_goal≈0.59m` point where `epsi` was still around `0.25-0.27rad`. Kept route geometry and speed-shaping baseline unchanged: `route_goal_extension_m=0.0`, `exit_alignment_path_length=10.0`, `exit_alignment_distance_after_goal=0.0`, `exit_alignment_post_clearance_goal_window=0.0`, `yield_stop_buffer_distance=8.0`, and `yield_release_clearance_margin=1.0`. Updated `run_lane_entry_heading_cost_check.sh` to write fresh `*_lane_entry_heading_cost_b11_check` result directories. | Local validation: tuning JSON valid; affected Python files compile; shell script syntax passes. Active tuning values: `completion_lane_entry_min_s_after_route_goal=1.0`, heading cost enabled `true`, goal window `8.0m`, weight `0.4`, max `|epsi|=0.35rad`, validated yield settings unchanged. | Re-run as fresh `lane_entry_heading_cost_b11_check`. Minimum acceptance remains required SMPC PASS, no footprint collision, valid yield order, `solver_failure_frac=0.000`, and no 600-step non-completion. The specific B1.1 success signal is lane-entry completion with `s_after_route_goal>=1.0m` and completion `epsi` lower than Stage B1, ideally below the Stage A baseline (`0.253/0.256rad`). If the run becomes unsafe or solver failures appear, revert only the weight to `0.2` first; do not reintroduce the rejected goal-anchored path shaping. |
| `20260705_120651_lane_entry_heading_cost_b11_check` | Server Stage B1.1 validation for the stronger bounded heading objective plus `s_after_route_goal>=1.0m` lane-entry completion delay. | Pulled fresh result directory via direct `scp -r`. Batch is complete with five subruns. `smpc_debug_setup.json` confirms B1.1 settings were active for required SMPC: heading cost enabled `true`, goal window `8.0m`, weight `0.4`, max `|epsi|=0.35rad`, and `completion_lane_entry_min_s_after_route_goal=1.0`. Debug steps show the heading objective activated after target clearance near the goal: var-risk `82` active steps, fixed-risk `84` active steps. | Gate `Overall: WARN` only because non-required `smpc_open_loop` violates yield order. Required SMPC both PASS with no footprint collision, yield OK, and `solver_failure_frac=0.000`. `smpc_var_risk`: completion step `274`, completion time `13.65s`, center dmin `4.147m`, `goal_dist=0.689m`, `ey=-1.278m`, `epsi=0.268rad`, `s_after_route_goal=1.590m`. `smpc_fixed_risk`: completion step `276`, completion time `13.75s`, center dmin `4.145m`, `goal_dist=0.648m`, `ey=-1.310m`, `epsi=0.262rad`, `s_after_route_goal=1.590m`. Stage A diagnostic relationship remains: `ref_minus_map_yaw=0.0`, with `ego_minus_ref_yaw` equal to the completion `epsi`. | Outcome: accept as safe but reject as a visual-heading fix. The completion delay worked, but the extra 1.0m and doubled heading-cost weight did not reduce heading error; compared with Stage A (`0.253/0.256rad`) and Stage B1 (`0.267/0.251rad`), B1.1 is worse for var-risk and worse than both baselines for fixed-risk. Do not increase this weight further and do not use B1.1 as the full-experiment mainline. This suggests the issue is not solved by a small quadratic heading penalty at the existing reference speed/curvature; the next heading-specific idea should be either a terminal/preview steering objective tied to the first completion-eligible horizon point or a locally speed-aware yaw-settling manoeuvre after target clearance. The full dissertation mainline remains `20260628_103325_final_dissertation`. |
| Local terminal/preview steering objective implementation after B1.1 | Implements the next heading-specific trial after B1/B1.1 showed that a weak ramped heading penalty is safe but ineffective. | Reused the existing `heading_cost_weights` solver parameter rather than adding a new solver structure. Added `lane_entry_heading_cost_mode`, `lane_entry_heading_cost_preview_weight`, `lane_entry_heading_cost_preview_back_steps`, and `lane_entry_heading_cost_preview_s_after_route_goal` to `SMPCAgent`, `VehicleParams`, debug setup, tuning config, and runner pass-through. In `preview_terminal` mode, the heading objective no longer spreads across all near-goal horizon points; it selects the first horizon state that is both inside the original-goal window and at least `1.0m` beyond the original route goal, applies terminal weight `2.0`, and applies preview weight `0.5` to one preceding horizon step. The activation remains bounded to target-cleared, near-goal, non-open-loop SMPC cases with `|epsi|<=0.35rad`. Added `run_lane_entry_preview_steering_check.sh` for fresh server validation. | Local validation: tuning JSON valid; `smpc_agent.py`, `run_intersection_scenario.py`, `mpc_utils.py`, and diagnostics summary compile; shell syntax passes. Active tuning values: mode `preview_terminal`, terminal weight `2.0`, preview weight `0.5`, preview back steps `1`, preview `s_after_route_goal=1.0`, completion lane-entry delay `1.0m`, validated yield settings unchanged. | Re-run as fresh `lane_entry_preview_steering_check`. Minimum acceptance remains required SMPC PASS, no footprint collision, valid yield order, `solver_failure_frac=0.000`, and no 600-step non-completion. Desired heading signal is completion `epsi` below Stage A (`0.253/0.256rad`) and below B1.1 (`0.268/0.262rad`). If safety or solver feasibility regresses, revert this preview mode before trying any speed-aware yaw-settling manoeuvre. |

## Current Full Dissertation-Batch Candidate: `20260627_212618_final_dissertation`

This run should be treated as the current best full dissertation-batch candidate for the rule-aware adaptive-risk SMPC direction.

Configuration:

- Scenario: right-hand-traffic unsignalised give-way interaction, ego left turn across an oncoming straight-going target.
- Visual geometry: ego `start_left_offset=+2.75`, confirmed by user video inspection as the correct start position.
- Policies in the validation run: `smpc_var_risk`, `smpc_fixed_risk`, `smpc_open_loop`, `notv`, `notv_cl`.
- Risk profile: `adaptive_interaction_severity`.
- Architecture: unified mild adaptive risk allocation, deterministic rule-yield SMPC solve bypass, bounded early `released_recovery` handoff bypass, `yield_release_clearance_margin=1.0m`, and `yield_stop_buffer_distance=8.0m`.
- Bypass condition: active priority rule-yield in `approach_yield_line` / `hold_yield_line`, or low-speed early recovery after the priority target has cleared the conflict zone.

Measured outcome:

- `smpc_var_risk`: completion valid, no footprint collision, target cleared before ego entered the conflict zone, `solver_failure_frac=0.000`, center clearance `3.897m`, min footprint separation `0.135m`, completion time `12.55s`.
- `smpc_fixed_risk`: completion valid, no footprint collision, target cleared before ego entered the conflict zone, `solver_failure_frac=0.000`, center clearance `4.142m`, min footprint separation `0.347m`, completion time `11.60s`.
- Both SMPC policies kept bounded deterministic bypass: fixed-risk `55` approach/hold frames + `16` recovery-handoff frames; var-risk `56` approach/hold frames + `16` recovery-handoff frames.
- `smpc_open_loop` is non-required and warns as expected: it avoids collision but violates yield order, showing why rule-aware yielding matters.

Why this matters:

- This is the first run that combines the user-confirmed visual start geometry, full adaptive-risk profile, full dissertation-batch baselines, generated paper panel, and passing required SMPC post-CARLA gate.
- It validates the final architecture direction: traffic rules handle deterministic priority-yield and short recovery-handoff phases, while SMPC/adaptive risk remains responsible for interaction-aware planning outside those deterministic windows.
- It fixes the hold-line footprint collision from `20260627_205856_final_dissertation` by moving the waiting pose upstream with `yield_stop_buffer_distance=8.0m`.

Known trade-off:

- Clearance is lower than the earlier `+1.85` zero-failure milestone (`~4.15-4.23m` instead of `~5.31-5.34m`), but remains collision-free and passes the post-CARLA gate.
- Var-risk footprint separation is still tight (`0.135m`), so do not reduce `yield_stop_buffer_distance` below `8.0m` unless a new visual constraint forces it.
- Do not broaden the recovery bypass unless a new failure mode appears; the bounded `16`-frame handoff is sufficient for the validated scenario.

Recommended use in the dissertation:

- Use `20260627_212618_final_dissertation` as the main final-method quantitative candidate.
- Use `20260627_201840` as an earlier passed candidate and `20260627_205856_final_dissertation` as evidence motivating the hold-line clearance fix.
- Frame the final contribution as rule-aware SMPC with interaction-severity-adaptive risk allocation and bounded deterministic rule-yield / recovery-handoff control.

## Earlier Passed Candidate: `20260627_201840`

This run remains useful as the first passed candidate with the user-confirmed `+2.75m` visual geometry, but it is superseded by `20260627_212618_final_dissertation` because the later run includes `smpc_open_loop`, generates paper-panel artifacts, and validates the `8.0m` hold-line clearance fix.

## Earlier Milestone: Dissertation Candidate Run `20260627_155115`

This run should be treated as the first dissertation-quality milestone for the rule-aware adaptive-risk SMPC direction, but it is no longer the current final-method candidate.

Configuration:

- Scenario: right-hand-traffic unsignalised give-way interaction, ego left turn across an oncoming straight-going target.
- Policies: `smpc_var_risk`, `smpc_fixed_risk`, `notv`, `notv_cl`.
- Risk profile: `adaptive_interaction_severity`.
- Architecture: unified mild adaptive risk allocation plus deterministic rule-yield SMPC solve bypass.
- Bypass condition: active rule-aware priority yield, valid priority prediction, and yield phase in `approach_yield_line` or `hold_yield_line`.

Measured outcome:

- `smpc_var_risk`: completion valid, no footprint collision, target cleared before ego entered the conflict zone, `solver_failure_frac=0.000`, center clearance `5.215m`, completion time `10.40s`.
- `smpc_fixed_risk`: completion valid, no footprint collision, target cleared before ego entered the conflict zone, `solver_failure_frac=0.000`, center clearance `5.340m`, completion time `10.45s`.
- Both SMPC policies bypassed exactly `51` deterministic yield frames: `17` approach frames and `34` hold frames.
- Adaptive risk remained active and interpretable: approach/hold tightening stayed above upstream (`~1.72` vs `1.64`), while released recovery relaxed to `Phi^-1(0.90)=1.282`.
- User video inspection result: video quality is good; the vehicle behaviour is acceptable as a milestone demonstration, with clear rule-compliant yielding and recovery through the intersection.

Why this matters:

- This is the first run where the dissertation method passes the strict required-policy post-CARLA gate.
- It validates the final architecture direction: traffic rules handle deterministic priority-yield phases, while SMPC/adaptive risk remains responsible for interaction-aware planning outside those deterministic stop/hold windows.
- It converts the previous recurring solver failures from an implementation liability into a defensible architecture argument: rule-supervised stop/hold frames are not meaningful SMPC optimisation attempts and should not be counted as failed stochastic planning solves.

Known trade-off:

- Clearance is lower than earlier high-clearance runs (`~5.2-5.3m` instead of `~6.3-6.4m`), but remains safe.
- Longitudinal jerk is higher (`~4.4-4.5`), so comfort smoothing may be explored later if video quality or dissertation analysis requires it.
- Do not undo the bypass architecture only to improve clearance or comfort; any further change should preserve the zero-failure gate pass and the observed good video behaviour.

Recommended use in the dissertation:

- Use `20260627_155115` as the main qualitative demonstration video and the first quantitative milestone of the proposed method.
- Compare it against earlier baselines:
  - `20260627_140331`: severity logging only, still solver-limited.
  - `20260627_143621`: strict adaptive risk, mechanically valid but too conservative for solver feasibility.
  - `20260627_150047`: mild adaptive risk, improved fixed-risk but not var-risk.
  - `20260627_152103`: policy-specific near-upstream var-risk map, rejected due to worse hold behaviour.
- Frame the final contribution as rule-aware SMPC with interaction-severity-adaptive risk allocation and deterministic rule-yield solve bypass.

## Rejected Directions

- `yield_stop_buffer_distance=5.0` with fast recovery: caused high solver failure (`var=0.165`).
- `yield_stop_buffer_distance=6.0`: made video less conservative but worsened `var_risk` solver failure.
- `yield_reference_decel=-3.0`: did not materially improve `var_risk` and worsened `fixed_risk`.
- `yield_reference_min_speed=1.0`: did not reduce `var_risk` and worsened `fixed_risk` hold failures.
- Increasing `yield_activation_distance` / `yield_observed_caution_distance`: not tested after user preference; avoid unless reference-profile tuning cannot reduce failures.

## Next Candidate Changes

Current dissertation candidate is `20260627_201840` (`risk_profile=adaptive_interaction_severity`, unified mild adaptive risk, deterministic rule-yield solve bypass, and bounded early recovery-handoff bypass). Earlier parameter-only baselines are retained for comparison but are no longer the primary direction.

1. If both policies remain around `0.06` to `0.08`, inspect first-failure KKT/debug setup before changing geometry again.
2. The `yield_brake_distance_margin=3.5` test was neutral (`20260621_215045`), so avoid further margin-only increases.
3. The `SMPC_MMPreds.A_MIN=-4.0` test improved fixed-risk (`20260621_221117`) but did not improve var-risk.
4. The `yield_reference_decel=-3.75` test improved var-risk but regressed fixed-risk slightly (`20260621_223615`).
5. Global `RefTrajGenerator A_MIN=-4.0` helped fixed-risk slightly but severely regressed var-risk (`20260621_230902`); do not keep it for both policies.
6. Policy-specific reference-generator bounds recovered var-risk and kept fixed-risk close to threshold (`20260622_223431`); keep this direction.
7. Next change should be architectural rather than another geometry/parameter nudge: bypass or gate SMPC solves during deterministic rule-supervised yield-stop/hold phases.
8. If another min-speed test is needed later, try `0.9`, not `1.0`.
9. After `20260627_140331`, use `adaptive_interaction_severity` before any further yield-geometry tuning; the logging-only run showed the severity signal is phase-correct.
10. After `20260627_143621`, the first adaptive-risk tightening is proven mechanically but too strict. Retune the risk mapping before changing geometry: lower approach/hold floors, or decouple deterministic rule-yield phases from stricter SMPC chance constraints.
11. The pending retune lowers floors and scales the tightening ramp; do not judge adaptive risk from the strict `0.72/0.78` floor run alone.
12. After `20260627_150047`, unified mild adaptive risk nearly passes fixed-risk but not var-risk. Next improvement should be policy-specific or solve-gating for deterministic yield phases; uniform tightening is not enough for var-risk.
13. The pending policy-specific map keeps the fixed-risk gain while making var-risk nearly upstream during deterministic yield; this is the next comparison before solve-gating.
14. After `20260627_152103`, reject the near-upstream var-risk map. Continue with solve-gating/bypass during deterministic yield phases rather than more risk-map-only tuning.
15. The pending solve-bypass change should be evaluated as part of the rule-aware architecture, not as a pure SMPC risk-map tweak.
16. After `20260627_155115`, solve bypass is validated: required policies pass the strict gate with zero solver failures. Future work should focus on comfort/clearance trade-off and broader baselines, not further solver-failure tuning.
17. The pending ego start offset change is only for visual lane correctness; do not interpret its effect as a change to adaptive risk allocation.
18. After `20260627_161450`, do not keep the raw `start_left_offset=-1.85`, `goal_left_offset=+1.85` geometry as final: it passes for var-risk but breaks fixed-risk completion. Preserve `20260627_155115` as the milestone until a lane-consistent geometry passes both SMPC policies.
19. After user screenshot review, `-1.85` is confirmed too far right; restore ego `start_left_offset=+1.85` and treat the red-rectangle position as the intended visual lane-centre start.
20. After `20260627_163523`, restored `+1.85` geometry passes both SMPC policies again. Pending only visual confirmation that the ego start matches the red rectangle; quantitatively this run is equivalent to the prior milestone and should become the final candidate if the video start is correct.
21. Latest screenshot shows `+1.85` is still slightly too far right; pending visual micro-adjustment is `start_left_offset=+2.75`. Validate visually before changing controller parameters.
22. `20260627_170746` accidentally/explicitly used `risk_profile=upstream_code`; its solver failures are expected because bypass is off. Use it as a baseline/paper-panel run, not as final-method validation.
23. After `20260627_194959`, `+2.75` with `adaptive_interaction_severity` is mechanically correct and safe, but fixed-risk narrowly fails due to `released_recovery` infeasibility. If visual placement is good, preserve the geometry and fix the recovery handoff rather than retuning approach/hold risk or conflict-zone parameters.
24. After user confirmation, `+2.75` is the correct visual start geometry. Keep it. The pending recovery-handoff bypass is the next validation target and should be judged by whether it removes the narrow fixed-risk recovery failures while preserving safety and yield order.
25. After `20260627_201840`, recovery-handoff bypass is validated: both required SMPC policies pass with `solver_failure_frac=0.000`, safety and yield order pass, and the bypass is bounded to `16` early recovery frames. Treat this as the current final-method dissertation candidate.
26. After `20260627_203901`, the remaining risk is footprint safety robustness, not solver feasibility. Add a target-clearance release buffer before `released_recovery`; do not increase recovery bypass or alter the user-confirmed `+2.75` geometry.
27. Local pending fix after `20260627_203901`: add `yield_release_clearance_margin=1.0m`. The expected effect is to delay release by about `0.17s`, covering the observed `0.15s` overlap while preserving all validated solver and geometry settings.
28. After `20260627_205856_final_dissertation`, `yield_release_clearance_margin=1.0m` is confirmed active but insufficient because fixed-risk collision occurs during `hold_yield_line`, before release. Next fix should increase `yield_stop_buffer_distance` upstream, likely `6.25m -> 8.0m`, rather than further increasing release buffer or recovery bypass.
29. Local pending fix after `20260627_205856_final_dissertation`: `yield_stop_buffer_distance=6.25m -> 8.0m`. This is a hold-line clearance fix, not a solver/risk/recovery change.
30. After `20260627_212618_final_dissertation`, the `8.0m` hold-line clearance fix is validated. Both required SMPC policies pass with zero solver failures, no footprint collision, valid yield order, and generated paper panel. Treat this as the current best full dissertation-batch candidate.
31. For the no-adaptive-risk ablation, use `rule_aware_static_risk`, not raw `upstream_code`. Raw upstream disables the rule-aware bypass and would confound the ablation.
32. After `20260628_153117_no_adaptive_risk_final_dissertation`, the no-adaptive-risk ablation is validated as a clean isolated comparison:
    - Command path: `run_give_way_no_adaptive_risk_ablation.sh`.
    - Risk profile: `rule_aware_static_risk`.
    - Constant SMPC risk setting: `tight=1.64`, `target_prob=0.949497`.
    - Rule-aware supervisor, deterministic yield bypass, bounded recovery handoff, `start_left_offset=+2.75`, `yield_stop_buffer_distance=8.0m`, and `yield_release_clearance_margin=1.0m` are unchanged from the final adaptive method.
    - Required policies pass the post-CARLA gate: `smpc_fixed_risk` center clearance `4.154m`, `smpc_var_risk` center clearance `4.285m`, no footprint collision, valid yield order, `solver_failure_frac=0.000`.
    - Compared with adaptive repeat `20260628_103325_final_dissertation`, static risk is not unsafe in this single scenario. The meaningful adaptive-risk benefit is mainly for `smpc_var_risk`: completion time improves from `12.70s` static to `11.55s` adaptive, forced reference linearization drops from `0.247` to `0.194`, and `max_abs_ey_debug` drops from `3.84m` to `3.15m`. Static risk keeps slightly larger center clearance for var-risk (`4.285m` vs `4.060m`) but is slower and less route-stable.
    - Debug trace confirms isolation: adaptive run has `adaptive_enabled=232/232` for var-risk with `tight` varying approximately `1.282-1.727`; static ablation has `adaptive_enabled=0/255` and `tight=1.64` for all var-risk frames while still logging interaction severity.
    - Dissertation interpretation: this ablation supports adaptive risk allocation as an efficiency/route-stability improvement over a rule-aware static-risk controller, while rule-awareness and deterministic yield supervision are the primary source of traffic-rule compliance and zero solver failures in the current single-scenario case.

33. After `20260705_160721_final_dissertation`, the first original paper initial condition (`ego_init_01`, `init_speed=9.364703726496288m/s`) fails for a different reason than the earlier tuned `6.0m/s` baseline:
    - All subruns execute normally; required SMPC policies complete with `solver_failure_frac=0.000`.
    - The post-CARLA gate fails because both required policies enter/occupy the conflict zone before the priority target clears it, causing footprint collision and yield-order failure.
    - Debug trace shows the observed target track is already available at step 1 and `braking_distance_required=True`, but the supervisor remains in `free_drive` until the ego is within the fixed observed-caution conflict-distance window. By that point, the vehicle is too close to the stop point for the configured `yield_stop_decel=-5.0m/s^2`.
    - Local fix: observed-track cautious approach now activates when either the fixed observed-caution conflict distance is met or the braking-distance trigger is met (`ego_distance_to_stop <= v^2/(2|yield_stop_decel|) + yield_brake_distance_margin`). This keeps the logic non-oracle while allowing high-speed paper initial conditions to brake as soon as the observed moving target implies a priority conflict.

34. After `20260705_164856_final_dissertation`, the observed-track trigger is confirmed active, but the high-speed paper initial condition still fails because the CARLA vehicle cannot stop before the conflict zone under the nominal `yield_stop_decel=-5.0m/s^2` command:
    - The controller enters `cautious_approach_observed_target` from step 1, so the issue is no longer late activation.
    - The ego still slides to approximately `dconf=1.09m`, inside the post-CARLA gate conflict radius, while the priority target has not cleared.
    - Local pending fix: add a bounded `yield_emergency_brake` fallback. It activates only during active yielding when the priority target has not cleared and the braking-distance trigger is already true. It ramps from the nominal yield command toward `yield_emergency_decel=-7.0m/s^2` under `yield_emergency_jerk_limit=15.0m/s^3`, and it is never allowed to make braking weaker than the nominal yield command.
    - Reproducibility/debug additions: `smpc_debug_setup.json` records all emergency-brake parameters; every debug step records emergency activation, previous acceleration, jerk-limited acceleration, final acceleration, safe conflict clearance, and distance to the emergency conflict-boundary stop target.
    - Validation target: rerun the high-speed paper `ego_init_01` single-init batch first. Do not run 50-init until both required SMPC policies pass completion, solver-failure, footprint-collision, and yield-order gates.

35. After `20260705_171739_final_dissertation`, the emergency fallback validates safety for the high-speed paper init, but the video exposes unacceptable naturalness and lane-entry issues:
    - Required SMPC policies pass post-CARLA safety/yield gates, but `max_longitudinal_jerk` is high and the ego hard-stops before the target is visible in the camera view.
    - Debug trace shows the ego stops at `dconf≈8.39m` and stays stopped until the target clears, then recovery ends too early; after recovery the vehicle slows to about `1m/s`, drifts laterally, and lane-entry diagnostics show it can reach the opposite lane.
    - Local pending fix: split yielding into rolling caution/creep and hard-stop phases. Observed-track activation may reduce speed early, but full hard-stop/emergency braking is delayed until the priority target is closer to the conflict zone (`yield_hard_stop_target_distance=8.0m`) or ego reaches the conflict-boundary threshold (`yield_hard_stop_conflict_distance=7.0m`).
    - To reduce jerk relative to the first emergency-brake trial, emergency braking is softened from `-7.0m/s^2` / `15.0m/s^3` to `yield_emergency_decel=-6.0m/s^2` and `yield_emergency_jerk_limit=8.0m/s^3`.
    - Post-yield recovery is extended and strengthened so the ego accelerates through the turn and lane-entry segment (`yield_recovery_steps=180`, `yield_recovery_speed=5.5m/s`, `yield_recovery_accel=1.8m/s^2`) instead of dropping back to a slow crawl before the lane entry completes.
    - New debug fields: every active-yield step records `hard_stop_required`, `hard_stop_target_close`, `hard_stop_conflict_close`, rolling speed target, and whether control/reference used rolling caution or hard-stop yielding.

36. After `20260705_174648_final_dissertation`, the post-turn/lane-entry fix is accepted but the delayed hard-stop settings fail yield safety:
    - The extended recovery is now treated as the correct post-turn solution. Lane-entry diagnostics show required SMPC policies enter the lane at about `5.3-5.8m/s`, with `|ey|<0.07m`, `|epsi|<0.01rad`, and `map_lane_id=1`.
    - The remaining failure is purely the give-way phase. Rolling caution prevents step-1 hard-stop behaviour, but it is too weak: at the last rolling step the ego still travels at about `8.22m/s`; hard-stop yielding begins around `dconf=6.28m`, which is physically too late.
    - Local pending fix: keep the accepted recovery values unchanged, strengthen the rolling phase (`yield_caution_speed=3.5m/s`, `yield_caution_decel=-4.0m/s^2`), and switch to hard-stop yielding earlier (`yield_hard_stop_target_distance=10.0m`, `yield_hard_stop_conflict_distance=11.0m`).
    - Emergency braking remains bounded at `yield_emergency_decel=-6.0m/s^2` and `yield_emergency_jerk_limit=8.0m/s^3`; the goal is to regain yield safety without reverting to the visually unacceptable step-1 emergency stop.

37. After `20260705_180748_final_dissertation`, the rolling phase and post-turn recovery remain useful, but the hard-stop trigger is still too late:
    - Required SMPC policies complete with zero solver failures and stable lane entry, but both still enter the conflict zone about `1.25s` before the target clears it.
    - Debug shows hard-stop yielding begins around `dconf=10.65m` with ego speed still about `8.32m/s`, then the vehicle stops around `dconf=2.16m`, inside the `4.0m` conflict radius.
    - Local pending fix: do not increase emergency deceleration yet. Instead, keep `yield_caution_speed=3.5m/s`, `yield_caution_decel=-4.0m/s^2`, `yield_emergency_decel=-6.0m/s^2`, and `yield_emergency_jerk_limit=8.0m/s^3`, but move hard-stop thresholds earlier to `yield_hard_stop_target_distance=12.0m` and `yield_hard_stop_conflict_distance=14.0m`.
    - Expected effect: hard-stop should start near `dconf≈14m`, adding roughly `3m` of braking distance while preserving the rolling-caution phase and the accepted post-turn recovery behaviour.

38. After `20260705_182730_final_dissertation`, the earlier hard-stop trigger improves timing but still leaves a small conflict-zone violation:
    - Required SMPC policies again complete with zero solver failures and stable lane entry.
    - Hard-stop yielding starts earlier (`step≈8`, `dconf≈13.04m`) but the vehicle still stops at `dconf≈3.21m`, about `0.8m` inside the `4.0m` conflict radius. Yield-order failure is now about `1.1-1.15s` early.
    - Local pending fix: keep `yield_hard_stop_target_distance=12.0m` and keep emergency braking at `yield_emergency_decel=-6.0m/s^2`; only increase `yield_hard_stop_conflict_distance` from `14.0m` to `15.5m` to add a small braking-distance margin without increasing jerk/comfort cost.

39. After `20260705_184623_final_dissertation`, user video review shows the remaining failure is not primarily an ego yield-stop clearance issue:
    - The ego visibly yields, and the post-CARLA gate reports valid yield order for both required SMPC policies.
    - The target vehicle is configured as `traffic_role=priority_oncoming_straight`, but it is controlled by the generic obstacle-avoiding `MPCAgent`. In the video and debug positions, the target turns/veers toward the stopped ego instead of continuing straight through the intersection.
    - This violates the scenario definition: the priority oncoming vehicle should continue straight, and the ego is responsible for yielding.
    - Local fix: introduce `StraightLineAgent` for the priority target and set the give-way scenario target `policy_type` to `straight`. The new target controller tracks the fixed start-to-goal straight line and does not perform evasive turns around the ego.
    - Additional reproducibility logging: `scenario_steps.csv` now records first-target position, speed, yaw, and control fields (`target0_*`) so future runs can verify target straightness without relying only on video review.

40. After `20260705_191146_final_dissertation`, the generic `MPCAgent` contamination is removed, but the first `StraightLineAgent` implementation still fails the target-straight validation:
    - Required SMPC policies complete and have no footprint collision (`min_center_distance≈6.10m`, `min_footprint_separation≈2.79m`), but the post-CARLA gate fails yield order because the target remains in the detected conflict zone long after ego enters.
    - The new `target0_*` logs show the target is still not straight: `target0_y_rhs` drifts from about `3.35m` to `-10m`, target yaw changes from about `180deg` to `127-178deg`, and `target0_steer` saturates near `±0.18`.
    - Root cause: the first `StraightLineAgent` lateral/heading feedback has the wrong effective sign under the CARLA/RHS steering convention, so it commands a turn instead of holding the priority target straight.
    - Local fix: simplify `StraightLineAgent` to zero steering with speed hold and goal-proximity braking. This is intentionally conservative for the baseline: the priority target now maintains its initial straight heading instead of performing lateral correction that can introduce another coordinate-sign failure.
    - Next validation run should first check `scenario_steps.csv` target fields: `target0_y_rhs` should remain near its initial value, `target0_yaw_deg` should stay near `180deg`, and `target0_steer` should remain near zero.

41. After `20260705_193245_final_dissertation`, the zero-steering priority target is mostly straight, but the true straight-line target path exposes an ego stop-line clearance issue:
    - For `smpc_var_risk`, `target0_steer=0.0`, `target0_yaw_deg` stays close to `180deg`, and `target0_y_rhs` follows the configured start-to-goal line from about `3.35m` to `2.87m`.
    - The run still fails because the ego hold position is too close to the straight-going target path: `smpc_var_risk` has footprint collision duration about `1.20s` and center distance about `2.705m`, with ego entering the conflict circle before the target clears.
    - Rejected follow-up: moving the target offset from `1.85m` to `2.20m` was the wrong visual direction, and increasing `yield_stop_buffer_distance` to `10.5m` made the desired stop line physically unreachable for the high-speed paper init.
    - Next validation should require the target to remain visually straight and slightly more roadside, `target0_steer≈0`, no footprint collision, valid yield order, and normal post-turn lane-entry diagnostics.

42. Local fix after `20260705_195344_final_dissertation`:
    - Target geometry correction: reduce the priority target straight-lane offset to `start_left_offset=goal_left_offset=1.50m`, because the previous `2.20m` moved the path in the wrong direction.
    - Target controller correction: keep `StraightLineAgent` at zero steering and remove conflict-proximal goal braking; the priority vehicle should continue straight through the intersection instead of stopping near the conflict zone.
    - Ego stop-line correction: first set `yield_stop_buffer_distance=5.5m`, not `10.5m`. With `conflict_s≈15.6m`, a `10.5m` buffer asks the ego to stop within roughly `5m` from the start while travelling about `9m/s`, which is not physically reachable. The `5.5m` stop line is still outside the `4.0m` conflict radius while leaving enough distance to brake.
    - Hard-stop trigger correction: `hard_stop_required` now includes `hard_stop_stop_line_braking = braking_distance_required and target_has_priority`, so the EV switches to hard-stop based on distance to the actual stop line rather than waiting for only target-distance or conflict-distance thresholds.
    - Next validation should check `hard_stop_stop_line_braking`, final `ego_distance_to_conflict`, target straightness (`target0_steer≈0`, stable `target0_y_rhs`), footprint collision, yield order, and lane-entry diagnostics.

43. Local fix after `20260705_201434_final_dissertation`:
    - The previous fix worked structurally: both required SMPC policies completed with zero solver failures and good lane-entry heading, but both still had about `1.10s` footprint collision and entered the conflict circle about `1.05s` before the straight target cleared.
    - Debug confirms `hard_stop_stop_line_braking=True` from step 1, so the new trigger is active; the remaining issue is stop-line clearance. The ego still stopped at `ego_distance_to_conflict≈3.21m`, inside the `4.0m` conflict radius.
    - Local pending fix: increase only `yield_stop_buffer_distance` from `5.5m` to `7.0m`. This should add about `1.5m` final clearance while avoiding the physically unreachable `10.5m` stop line. Keep target offset `1.50m`, `StraightLineAgent` zero steering, hard-stop trigger logic, emergency decel, and accepted post-turn recovery unchanged.

44. Local fix after `20260705_203724_final_dissertation`:
    - The `7.0m` stop buffer improved rule order for `smpc_var_risk`, but both required SMPC policies still had about `1.05s` footprint collision. This means the center-circle yield criterion can pass while CARLA footprint replay still overlaps the straight target path.
    - Do not keep blindly increasing `yield_stop_buffer_distance`; leave it at `7.0m`.
    - Add `yield_footprint_clearance_margin=1.5m` as an explicit footprint-aware clearance margin. Target release clearance now uses `yield_conflict_radius + max(yield_release_clearance_margin, yield_footprint_clearance_margin)`, and emergency safe-stop diagnostics use at least `yield_conflict_radius + yield_footprint_clearance_margin`.
    - New debug fields include `ego_required_clearance`, `ego_inside_footprint_clearance`, and `footprint_clearance_margin`, so the next run can distinguish residual low-level braking overshoot from an overly early target-release decision.
